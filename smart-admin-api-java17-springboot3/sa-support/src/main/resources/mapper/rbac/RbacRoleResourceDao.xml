<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.lab1024.sa.base.module.support.rbac.dao.RbacRoleResourceDao">

    <!-- 根据角色ID列表查询资源编码列表 -->
    <select id="getResourceCodesByRoleIds" resultType="java.lang.String">
        SELECT DISTINCT res.resource_code
        FROM t_rbac_role_resource rr
        INNER JOIN t_rbac_resource res ON rr.resource_id = res.resource_id
        WHERE rr.role_id IN
        <foreach collection="roleIds" item="roleId" open="(" separator="," close=")">
            #{roleId}
        </foreach>
          AND rr.status = 1
          AND res.deleted_flag = 0
          AND res.status = 1
    </select>

</mapper>

