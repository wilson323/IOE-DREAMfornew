<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.lab1024.sa.base.module.support.rbac.dao.RbacResourceDao">

    <!-- 根据资源编码查询资源是否存在 -->
    <select id="existsByResourceCode" resultType="boolean">
        SELECT COUNT(1) > 0
        FROM t_rbac_resource
        WHERE resource_code = #{resourceCode}
          AND deleted_flag = 0
        LIMIT 1
    </select>

    <!-- 根据角色ID列表查询权限编码列表 -->
    <select id="getPermissionCodesByRoleIds" resultType="java.lang.String">
        SELECT DISTINCT res.resource_code
        FROM t_rbac_role_resource rr
        INNER JOIN t_rbac_resource res ON rr.resource_id = res.resource_id
        WHERE rr.role_id IN
        <foreach collection="roleIds" item="roleId" open="(" separator="," close=")">
            #{roleId}
        </foreach>
          AND rr.status = 1
          AND res.deleted_flag = 0
          AND res.status = 1
    </select>

</mapper>

