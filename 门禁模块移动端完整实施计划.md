# 门禁模块移动端完整实施计划

**文档版本**: v1.0.0
**制定日期**: 2025-01-30
**制定人**: IOE-DREAM Team
**目标**: 确保门禁模块移动端完整企业级实现，与业务文档100%对齐

---

## 📊 一、门禁模块业务需求分析

### 1.1 核心业务模块（7大模块）

基于 `documentation/业务模块/各业务模块文档/门禁/` 文档分析：

#### 模块1: 设备管理模块
**功能清单**:
- ✅ 设备列表查询（已实现）
- ❌ 新增设备（Pull/Push设备）
- ❌ 删除设备
- ❌ 搜索设备（自动网络搜索）
- ❌ 设备控制（重启、启用/禁用、清除命令、固件升级）
- ❌ 高级设置（验证参数、时区、通讯参数、NTP服务）
- ❌ 查看信息（设备参数、人员信息、记录信息、门禁规则、设备容量）
- ❌ 通信设置（修改IP、密码、网络连接方式）
- ❌ 区域关联（设备必须关联区域）

#### 模块2: 区域空间管理模块
**功能清单**:
- ✅ 区域列表查询（已实现）
- ❌ 创建区域
- ❌ 编辑区域
- ❌ 删除区域
- ❌ 区域权限分配
- ❌ 设备分配到区域
- ❌ 区域占用监控

#### 模块3: 实时监控模块
**功能清单**:
- ✅ 实时监控页面（已实现）
- ❌ 实时状态监控（设备状态、门状态、读头状态）
- ❌ 报警处理（实时报警、报警历史）
- ❌ 视频联动（门禁触发时调取视频）
- ❌ 人员追踪（实时位置追踪）

#### 模块4: 事件记录查询模块
**功能清单**:
- ✅ 通行记录查询（已实现）
- ❌ 异常事件处理（异常标记、异常处理流程）
- ❌ 统计报表生成（日报、周报、月报）
- ❌ 事件详情查看
- ❌ 事件导出

#### 模块5: 审批流程管理模块
**功能清单**:
- ❌ 权限申请（员工申请门禁权限）
- ❌ 访客预约（访客预约申请）
- ❌ 紧急权限处理（临时权限授权）
- ❌ 审批流程配置（配置审批规则）

#### 模块6: 系统配置模块
**功能清单**:
- ❌ 系统参数配置
- ❌ 用户权限管理
- ❌ 许可证管理
- ❌ 备份恢复

#### 模块7: 高级功能模块
**功能清单**:
- ❌ 全局反潜（防止尾随进入）
- ❌ 全局联动（多门联动控制）
- ❌ 全局互锁（互斥区域控制）
- ❌ 疏散点管理（紧急疏散）
- ❌ 人数控制（区域人数限制）
- ❌ 人员限制（特定人员权限限制）

### 1.2 移动端当前实现情况

#### 已实现功能（4个页面）
1. **access/record.vue** - 通行记录查询
   - 通行记录列表
   - 统计概览（今日通行、本周通行、成功率）
   - 快速筛选（全部、成功、失败、今天）
   - 下拉刷新、上拉加载更多

2. **access/device.vue** - 设备管理
   - 设备列表展示
   - 设备在线/离线状态
   - 设备基本信息

3. **access/monitor.vue** - 实时监控
   - 监控页面框架

4. **access/permission.vue** - 权限管理
   - 权限管理框架

#### 未实现功能分析

**缺失核心功能**（按优先级排序）:

**P0级（必须实现）**:
1. ❌ 设备完整管理（新增、删除、控制、高级设置）
2. ❌ 区域完整管理（创建、编辑、删除、权限分配）
3. ❌ 审批流程（权限申请、访客预约、审批处理）
4. ❌ 门禁远程控制（远程开门、常开、常闭）
5. ❌ 事件详情查看
6. ❌ 异常事件处理

**P1级（重要功能）**:
7. ❌ 实时状态监控完善
8. ❌ 报警处理
9. ❌ 统计报表
10. ❌ 高级功能（反潜、联动、互锁）

**P2级（增强功能）**:
11. ❌ 视频联动
12. ❌ 人员追踪
13. ❌ 系统配置
14. ❌ 数据导出

---

## 🎯 二、门禁移动端详细实施计划

### 2.1 第1阶段：核心管理功能（P0级）

#### 任务1.1: 设备管理完整实现
**预计工作量**: 3天

**页面清单**:
1. `access/device.vue` - 设备列表（增强）
   - 功能：
     - ✅ 设备列表展示
     - ✅ 在线/离线状态
     - ❌ 新增设备按钮（Pull/Push）
     - ❌ 搜索设备按钮（网络搜索）
     - ❌ 批量操作（删除、启用/禁用）
     - ❌ 下拉刷新
     - ❌ 设备筛选（按区域、按状态）

2. `access/device-add.vue` - 新增设备页面
   - 表单字段：
     - 设备名称
     - 设备类型（门禁控制器、门禁一体机）
     - 设备型号
     - 通讯方式（TCP/IP、RS485）
     - IP地址/端口
     - 关联区域（必选）
     - 设备位置描述
   - 验证规则：
     - 设备名称：必填，2-50字符
     - IP地址：必填且格式正确
     - 区域：必选
   - API调用：
     - 调用设备管理API创建设备
     - 成功后自动同步门和读头信息

3. `access/device-detail.vue` - 设备详情页面
   - 功能：
     - 设备基本信息展示
     - 门信息列表（自动推送）
     - 读头信息列表（自动推送）
     - 设备状态（在线/离线）
     - 快速操作：
       - 远程开门
       - 重启设备
       - 启用/禁用
       - 同步数据
     - 高级设置入口：
       - 验证参数设置
       - 时区设置
       - NTP服务器设置
     - 查看信息入口：
       - 设备参数
       - 人员信息
       - 通行记录
       - 设备容量

4. `access/device-control.vue` - 设备远程控制
   - 功能：
     - 远程开门（单次、常开、常闭）
     - 重启设备
     - 启用/禁用设备
     - 清除命令缓存
     - 同步时间
   - 安全确认：
     - 重要操作需二次确认
     - 操作记录日志

5. `access/device-advanced.vue` - 设备高级设置
   - 功能：
     - 下发验证参数
     - 设置时区
     - 修改IP地址
     - 修改通讯密码
     - NTP服务器设置
     - 设备替换
     - 固件升级

**API接口清单**:
```typescript
// 设备管理API
interface DeviceManagementAPI {
  // 设备列表
  getDeviceList(params: DeviceQueryForm): Promise<PageResult<DeviceInfo>>;

  // 新增设备
  addDevice(form: DeviceAddForm): Promise<DeviceInfo>;

  // 搜索设备
  searchDevices(): Promise<DeviceInfo[]>;

  // 删除设备
  deleteDevice(deviceId: number): Promise<void>;

  // 设备详情
  getDeviceDetail(deviceId: number): Promise<DeviceDetailVO>;

  // 远程控制
  remoteOpenDoor(doorId: number): Promise<void>;
  remoteKeepOpen(doorId: number): Promise<void>;
  remoteKeepClosed(doorId: number): Promise<void>;
  rebootDevice(deviceId: number): Promise<void>;
  enableDevice(deviceId: number): Promise<void>;
  disableDevice(deviceId: number): Promise<void>;

  // 高级设置
  setVerifyParam(deviceId: number, param: VerifyParam): Promise<void>;
  setTimezone(deviceId: number, timezone: string): Promise<void>;
  setNTPServer(deviceId: number, ntp: NTPConfig): Promise<void>;
  updateDeviceIP(deviceId: number, ip: string): Promise<void>;
  upgradeFirmware(deviceId: number, file: File): Promise<void>;

  // 查看信息
  getDeviceParams(deviceId: number): Promise<DeviceParams>;
  getDevicePersons(deviceId: number): Promise<PersonInfo[]>;
  getDeviceRecords(deviceId: number): Promise<AccessRecord[]>;
  getDeviceCapacity(deviceId: number): Promise<DeviceCapacity>;
}
```

#### 任务1.2: 区域管理完整实现
**预计工作量**: 2天

**页面清单**:
1. `access/area.vue` - 区域列表（增强）
   - 功能：
     - ✅ 区域列表展示
     - ❌ 新增区域按钮
     - ❌ 区域树形展示
     - ❌ 区域搜索
     - ❌ 区域筛选（按状态、按类型）

2. `access/area-add.vue` - 新增区域页面
   - 表单字段：
     - 区域名称
     - 区域类型（普通区域、机密区域、危险区域）
     - 父级区域（支持多级区域）
     - 区域描述
     - 位置描述
     - 关联设备
     - 最大人数限制
     - 需要授权（是否需要审批）
   - 验证规则：
     - 区域名称：必填，2-50字符
     - 区域类型：必选
     - 最大人数：必须大于0

3. `access/area-detail.vue` - 区域详情页面
   - 功能：
     - 区域基本信息
     - 关联设备列表
     - 人员权限列表
     - 实时占用人数
     - 快速操作：
       - 添加设备
       - 添加人员
       - 编辑区域
       - 删除区域

4. `access/area-permission.vue` - 区域权限分配
   - 功能：
     - 人员列表
     - 批量选择人员
     - 设置权限有效期
     - 设置通行时段
     - 权限类型选择（永久、临时）

**API接口清单**:
```typescript
// 区域管理API
interface AreaManagementAPI {
  // 区域列表
  getAreaList(params: AreaQueryForm): Promise<PageResult<AreaInfo>>;

  // 新增区域
  addArea(form: AreaAddForm): Promise<AreaInfo>;

  // 编辑区域
  updateArea(areaId: number, form: AreaUpdateForm): Promise<void>;

  // 删除区域
  deleteArea(areaId: number): Promise<void>;

  // 区域详情
  getAreaDetail(areaId: number): Promise<AreaDetailVO>;

  // 区域树形结构
  getAreaTree(): Promise<AreaTreeNode[]>;

  // 添加设备到区域
  addDeviceToArea(areaId: number, deviceId: number): Promise<void>;

  // 移除区域设备
  removeDeviceFromArea(areaId: number, deviceId: number): Promise<void>;

  // 分配人员权限
  assignPersonPermission(areaId: number, form: PermissionAssignForm): Promise<void>;

  // 移除人员权限
  removePersonPermission(areaId: number, personId: number): Promise<void>;

  // 区域占用统计
  getAreaOccupancy(areaId: number): Promise<AreaOccupancyVO>;
}
```

#### 任务1.3: 审批流程管理
**预计工作量**: 2.5天

**页面清单**:
1. `access/approval.vue` - 审批列表
   - 功能：
     - 审批类型Tab（权限申请、访客预约、紧急权限）
     - 待审批列表
     - 已审批列表
     - 我的申请列表
     - 搜索和筛选
     - 下拉刷新

2. `access/permission-apply.vue` - 权限申请页面
   - 表单字段：
     - 申请标题
     - 申请类型（新增权限、权限延期、权限扩展）
     - 目标区域（可多选）
     - 通行时段（工作日、周末、全天、自定义）
     - 权限有效期（开始时间、结束时间）
     - 申请原因
     - 附件（可选）
   - 提交流程：
     - 提交申请
     - 进入审批流程
     - 实时审批进度查询

3. `access/visitor-appointment.vue` - 访客预约页面
   - 表单字段：
     - 访客姓名
     - 访客手机号
     - 访客身份证号（可选）
     - 访客单位
     - 访问事由
     - 访问区域（可多选）
     - 访问时间（开始时间、结束时间）
     - 接待人
     - 是否需要人脸采集
   - 特殊功能：
     - 生成访客二维码
     - 访客人脸采集
     - 访客门禁授权

4. `access/approval-detail.vue` - 审批详情页面
   - 功能：
     - 申请信息展示
     - 审批进度时间线
     - 审批意见列表
     - 审批操作（通过、驳回）
     - 审批历史记录

5. `access/emergency-permission.vue` - 紧急权限处理
   - 功能：
     - 紧急授权申请
     - 快速通道（一键授权）
     - 临时权限（限时生效）
     - 紧急联系人通知
     - 授权记录查询

**API接口清单**:
```typescript
// 审批流程API
interface ApprovalManagementAPI {
  // 审批列表
  getApprovalList(params: ApprovalQueryForm): Promise<PageResult<ApprovalInfo>>;

  // 权限申请
  submitPermissionApply(form: PermissionApplyForm): Promise<number>;

  // 访客预约
  submitVisitorAppointment(form: VisitorAppointmentForm): Promise<number>;

  // 紧急权限申请
  submitEmergencyPermission(form: EmergencyPermissionForm): Promise<number>;

  // 审批操作
  approveRequest(approvalId: number, comment: string): Promise<void>;
  rejectRequest(approvalId: number, reason: string): Promise<void>;

  // 审批详情
  getApprovalDetail(approvalId: number): Promise<ApprovalDetailVO>;

  // 审批进度
  getApprovalProgress(approvalId: number): Promise<ApprovalProgress[]>;

  // 我的申请
  getMyApplications(params: ApplicationQueryForm): Promise<PageResult<ApplicationInfo>>;

  // 访客二维码
  generateVisitorQRCode(appointmentId: number): Promise<string>;
}
```

#### 任务1.4: 门禁远程控制
**预计工作量**: 1.5天

**页面清单**:
1. `access/remote-control.vue` - 远程控制主页面
   - 功能：
     - 快速开门区域选择
     - 常用门快捷按钮
     - 批量开门
     - 常开/常闭控制
     - 操作日志记录
   - 安全机制：
     - 操作确认对话框
     - 操作原因记录
     - 操作权限验证
     - 操作超时自动恢复

2. `access/door-control.vue` - 单门控制页面
   - 功能：
     - 门状态实时显示
     - 开门按钮（单次、常开、常闭）
     - 门状态指示灯（绿色-正常、红色-异常）
     - 实时视频预览（可选）

**API接口清单**:
```typescript
// 远程控制API
interface RemoteControlAPI {
  // 快速开门
  quickOpenDoor(doorId: number, reason: string): Promise<void>;

  // 常开
  keepDoorOpen(doorId: number, reason: string): Promise<void>;

  // 常闭
  keepDoorClosed(doorId: number, reason: string): Promise<void>;

  // 恢复正常
  restoreDoor(doorId: number): Promise<void>;

  // 批量开门
  batchOpenDoors(doorIds: number[], reason: string): Promise<void>;

  // 门状态查询
  getDoorStatus(doorId: number): Promise<DoorStatus>;

  // 操作日志
  getControlLog(params: LogQueryForm): Promise<PageResult<ControlLog>>;
}
```

### 2.2 第2阶段：查询统计功能（P1级）

#### 任务2.1: 事件详情与异常处理
**预计工作量**: 1.5天

**页面清单**:
1. `access/record-detail.vue` - 事件详情页面
   - 功能：
     - 事件完整信息展示
     - 人员信息
     - 设备信息
     - 区域信息
     - 抓拍图片（如有）
     - 异常原因说明
     - 处理建议
     - 相关事件列表

2. `access/abnormal-handle.vue` - 异常处理页面
   - 功能：
     - 异常事件列表
     - 异常类型分类
     - 处理操作：
       - 标记为已处理
       - 添加处理说明
       - 关联异常分析
     - 处理记录

**API接口清单**:
```typescript
// 事件详情与异常处理API
interface EventDetailAPI {
  // 事件详情
  getEventDetail(eventId: number): Promise<EventDetailVO>;

  // 异常事件列表
  getAbnormalEvents(params: AbnormalQueryForm): Promise<PageResult<AbnormalEvent>>;

  // 处理异常
  handleAbnormal(eventId: number, handle: string): Promise<void>;

  // 标记已处理
  markAsProcessed(eventId: number): Promise<void>;

  // 相关事件
  getRelatedEvents(eventId: number): Promise<AccessRecord[]>;
}
```

#### 任务2.2: 统计报表
**预计工作量**: 2天

**页面清单**:
1. `access/statistics.vue` - 统计概览页面
   - 功能：
     - 6个统计卡片（今日通行、本周通行、异常次数、设备在线率、区域占用、人员活跃度）
     - 趋势图表（7天、30天、90天）
     - 实时数据刷新

2. `access/report-daily.vue` - 日报表页面
   - 功能：
     - 日期选择
     - 日报表数据表格
     - 数据导出

3. `access/report-monthly.vue` - 月报表页面
   - 功能：
     - 月份选择
     - 月度数据统计
     - 同比分析
     - 数据导出

**API接口清单**:
```typescript
// 统计报表API
interface StatisticsAPI {
  // 统计概览
  getStatisticsOverview(params: StatsQueryForm): Promise<StatisticsOverview>;

  // 趋势图表数据
  getTrendChart(params: TrendQueryForm): Promise<ChartVO>;

  // 日报表
  getDailyReport(date: string): Promise<DailyReportVO>;

  // 月报表
  getMonthlyReport(month: string): Promise<MonthlyReportVO>;

  // 区域统计
  getAreaStatistics(areaId: number, params: StatsQueryForm): Promise<AreaStatsVO>;

  // 设备统计
  getDeviceStatistics(deviceId: number, params: StatsQueryForm): Promise<DeviceStatsVO>;

  // 导出报表
  exportReport(reportType: string, params: any): Promise<Blob>;
}
```

### 2.3 第3阶段：高级功能（P2级）

#### 任务3.1: 实时监控完善
**预计工作量**: 2天

**页面清单**:
1. `access/monitor.vue` - 实时监控（增强）
   - 功能：
     - 设备状态监控大屏
     - 门状态实时显示
     - 实时事件滚动
     - 实时报警提示
     - 报警声音提醒
     - 快速处理入口

2. `access/alarm.vue` - 报警处理页面
   - 功能：
     - 实时报警列表
     - 报警类型筛选
     - 报警确认处理
     - 报警历史查询
     - 报警统计

**API接口清单**:
```typescript
// 实时监控API
interface MonitorAPI {
  // 实时状态
  getRealTimeStatus(): Promise<RealTimeStatus>;

  // 实时事件流
  getRealTimeEvents(): WebSocket;

  // 实时报警
  getRealTimeAlarms(): WebSocket;

  // 确认报警
  confirmAlarm(alarmId: number): Promise<void>;

  // 忽略报警
  ignoreAlarm(alarmId: number, reason: string): Promise<void>;
}
```

#### 任务3.2: 高级功能实现
**预计工作量**: 3天

**页面清单**:
1. `access/anti-passback.vue` - 反潜功能
2. `access/linkage.vue` - 联动控制
3. `access/interlock.vue` - 互锁功能
4. `access/evacuation.vue` - 疏散管理
5. `access/capacity.vue` - 人数控制

**API接口清单**:
```typescript
// 高级功能API
interface AdvancedFeatureAPI {
  // 全局反潜
  setAntiPassback(enabled: boolean, areaId?: number): Promise<void>;

  // 全局联动
  setLinkage(rule: LinkageRule): Promise<number>;

  // 全局互锁
  setInterlock(rule: InterlockRule): Promise<number>;

  // 疏散点
  setEvacuationPoint(pointId: number): Promise<void>;

  // 人数控制
  setCapacityControl(areaId: number, maxCapacity: number): Promise<void>;

  // 人员限制
  setPersonRestriction(restriction: PersonRestriction): Promise<number>;
}
```

---

## 📐 三、移动端开发规范

### 3.1 技术栈标准

```yaml
# 移动端技术栈
框架: uni-app 3.0+
语言: Vue 3.2+
UI组件: uni-ui 1.5+
状态管理: Pinia 2.0+
图标: uni-icons
构建工具: HBuilderX / Vue CLI
样式预处理: Sass/SCSS
代码规范: ESLint + Prettier
```

### 3.2 代码规范

#### 文件命名规范
```
pages/
  access/
    index.vue              # 列表页
    add.vue                # 新增页面
    edit.vue               # 编辑页面
    detail.vue             # 详情页面
    components/            # 组件目录
      door-card.vue        # 门卡片组件
```

#### 组件结构规范
```vue
<template>
  <view class="page-container">
    <!-- 页面内容 -->
  </view>
</template>

<script setup>
import { ref, reactive, computed, onMounted } from 'vue'
import { onLoad, onShow, onReachBottom } from '@dcloudio/uni-app'

// 响应式数据
const loading = ref(false)
const dataList = ref([])

// 生命周期
onLoad(() => {
  initPage()
})

// 方法定义
const initPage = () => {
  // 初始化逻辑
}
</script>

<style lang="scss" scoped>
.page-container {
  // 样式定义
}
</style>
```

### 3.3 API调用规范

```typescript
// API定义
// @/api/business/access/access-api.ts
import { request } from '@/utils/request'

export const accessApi = {
  // 获取设备列表
  getDeviceList: (params: DeviceQueryForm) => {
    return request<PageResult<DeviceInfo>>('/api/v1/access/device/list', params, 'GET')
  },

  // 新增设备
  addDevice: (form: DeviceAddForm) => {
    return request<DeviceInfo>('/api/v1/access/device/add', form, 'POST')
  }
}
```

### 3.4 状态管理规范

```typescript
// store/modules/access.ts
import { defineStore } from 'pinia'

export const useAccessStore = defineStore('access', {
  state: () => ({
    deviceList: [],
    currentDevice: null
  }),

  getters: {
    onlineDevices: (state) => {
      return state.deviceList.filter(d => d.online)
    }
  },

  actions: {
    async fetchDevices() {
      // 获取设备列表
    }
  }
})
```

### 3.5 全局一致性规范

#### 与考勤模块移动端保持一致：
1. **导航栏样式统一** - 使用相同的自定义导航栏组件
2. **卡片样式统一** - 统一的卡片阴影、圆角、渐变色
3. **列表样式统一** - 统一的列表项样式、点击效果
4. **按钮样式统一** - 统一的主按钮、次按钮、危险按钮样式
5. **状态指示统一** - 统一的成功、失败、警告状态颜色
6. **加载状态统一** - 统一的loading组件、空状态组件
7. **筛选功能统一** - 统一的筛选弹窗、筛选标签样式

#### 参考考勤模块已实现的优秀设计：
- **统计卡片** - 借鉴考勤统计的卡片设计
- **记录列表** - 借鉴通行记录的列表布局
- **筛选功能** - 借鉴考勤筛选的交互设计
- **加载更多** - 借鉴上拉加载更多的实现

### 3.6 性能优化规范

1. **按需加载** - 页面组件按需导入
2. **列表分页** - 使用分页或虚拟滚动
3. **图片懒加载** - 图片使用懒加载
4. **防抖节流** - 频繁操作使用防抖
5. **缓存策略** - 使用本地缓存减少请求
6. **WebSocket优化** - 实时监控使用长连接

---

## 🚀 四、实施步骤

### Phase 1: 核心管理功能（第1-2周）

**Week 1**:
- Day 1-3: 设备管理完整实现
  - 设备列表增强
  - 新增设备页面
  - 设备详情页面
  - 设备控制页面

**Week 2**:
- Day 1-2: 区域管理完整实现
  - 区域列表增强
  - 新增区域页面
  - 区域详情页面
  - 区域权限分配
- Day 3-5: 审批流程管理
  - 审批列表
  - 权限申请
  - 访客预约
  - 审批详情
  - 紧急权限

### Phase 2: 远程控制与查询统计（第3周）

**Week 3**:
- Day 1-1.5: 门禁远程控制
- Day 1.5-3: 事件详情与异常处理
- Day 3-5: 统计报表

### Phase 3: 高级功能（第4周）

**Week 4**:
- Day 1-2: 实时监控完善
- Day 2-4: 高级功能实现
- Day 4-5: 集成测试与优化

---

## ✅ 五、验收标准

### 功能验收标准
- [ ] 所有P0级功能100%实现
- [ ] 所有P1级功能100%实现
- [ ] P2级功能实现80%以上
- [ ] 所有功能通过测试

### 质量验收标准
- [ ] 代码符合ESLint规范
- [ ] TypeScript类型定义完整
- [ ] 组件复用率>60%
- [ ] 页面加载时间<2秒
- [ ] 操作响应时间<500ms

### 文档验收标准
- [ ] 所有页面有完整注释
- [ ] API接口文档完整
- [ ] 实施报告完整
- [ ] 用户使用手册完整

---

## 📝 六、注意事项

### 6.1 业务注意事项
1. **设备区域关联** - 设备必须关联到区域，这是核心业务逻辑
2. **权限审批流程** - 所有权限变更必须经过审批
3. **安全控制** - 远程开门等敏感操作必须有二次确认
4. **数据一致性** - 确保移动端与PC端数据一致

### 6.2 技术注意事项
1. **实时性保证** - 实时监控使用WebSocket
2. **离线处理** - 考虑离线场景的数据缓存
3. **跨平台兼容** - 确保iOS/Android兼容性
4. **性能优化** - 大数据量使用分页和虚拟滚动

### 6.3 全局一致性注意事项
1. **样式一致** - 严格参考考勤模块移动端的样式规范
2. **交互一致** - 保持与考勤模块相同的交互体验
3. **命名一致** - 组件、方法、变量命名规范统一
4. **代码结构一致** - 目录结构、代码组织方式统一

---

**文档状态**: 待审核
**下一步**: 开始第1阶段实施

**实施团队**: IOE-DREAM Team
**审核人**: 待定
**版本**: v1.0.0
