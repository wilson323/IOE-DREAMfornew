<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.lab1024.sa.common.workflow.dao.ApprovalConfigDao">

    <!-- 根据业务类型查询审批配置 -->
    <select id="selectByBusinessType" resultType="net.lab1024.sa.common.workflow.entity.ApprovalConfigEntity">
        SELECT *
        FROM t_common_approval_config
        WHERE business_type = #{businessType}
          AND status = 'ENABLED'
          AND (effective_time IS NULL OR effective_time &lt;= NOW())
          AND (expire_time IS NULL OR expire_time &gt;= NOW())
          AND deleted_flag = 0
        ORDER BY sort_order DESC, create_time DESC
        LIMIT 1
    </select>

    <!-- 根据业务类型和模块查询审批配置 -->
    <select id="selectByBusinessTypeAndModule" resultType="net.lab1024.sa.common.workflow.entity.ApprovalConfigEntity">
        SELECT *
        FROM t_common_approval_config
        WHERE business_type = #{businessType}
          AND module = #{module}
          AND status = 'ENABLED'
          AND (effective_time IS NULL OR effective_time &lt;= NOW())
          AND (expire_time IS NULL OR expire_time &gt;= NOW())
          AND deleted_flag = 0
        ORDER BY sort_order DESC, create_time DESC
        LIMIT 1
    </select>

    <!-- 检查业务类型是否存在 -->
    <select id="existsByBusinessType" resultType="int">
        SELECT COUNT(1)
        FROM t_common_approval_config
        WHERE business_type = #{businessType}
          AND deleted_flag = 0
    </select>

</mapper>
