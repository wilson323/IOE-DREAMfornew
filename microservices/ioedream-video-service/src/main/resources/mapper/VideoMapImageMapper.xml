<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.lab1024.sa.video.dao.VideoMapImageDao">

    <!-- 查询区域地图 -->
    <select id="selectByAreaId" resultType="net.lab1024.sa.video.entity.VideoMapImageEntity">
        SELECT * FROM t_video_map_image
        WHERE area_id = #{areaId}
          AND deleted_flag = 0
          AND status = 1
        ORDER BY floor_level, is_default DESC
    </select>

    <!-- 查询楼层地图 -->
    <select id="selectByFloor" resultType="net.lab1024.sa.video.entity.VideoMapImageEntity">
        SELECT * FROM t_video_map_image
        WHERE area_id = #{areaId}
          AND floor_level = #{floorLevel}
          AND deleted_flag = 0
          AND status = 1
        LIMIT 1
    </select>

    <!-- 查询默认地图 -->
    <select id="selectDefaultMap" resultType="net.lab1024.sa.video.entity.VideoMapImageEntity">
        SELECT * FROM t_video_map_image
        WHERE area_id = #{areaId}
          AND is_default = 1
          AND deleted_flag = 0
          AND status = 1
        LIMIT 1
    </select>

    <!-- 查询启用状态的地图 -->
    <select id="selectActiveMaps" resultType="net.lab1024.sa.video.entity.VideoMapImageEntity">
        SELECT * FROM t_video_map_image
        WHERE deleted_flag = 0
          AND status = 1
        ORDER BY area_id, floor_level
    </select>

</mapper>
