# IOE-DREAM 开发环境搭建指南

**目标**: 帮助开发者快速搭建完整的本地开发环境
**预计时间**: 30-60分钟
**适用系统**: Windows 10/11, macOS, Linux

---

## 📋 环境要求概览

### 必需软件
| 软件 | 版本要求 | 说明 |
|------|---------|------|
| **Java** | OpenJDK 17+ | 核心开发语言 |
| **Maven** | 3.8+ | 项目构建工具 |
| **MySQL** | 8.0+ | 主数据库 |
| **Redis** | 6.0+ | 缓存数据库 |
| **Node.js** | 18+ | 前端开发(可选) |
| **Git** | 2.30+ | 版本控制 |

### 推荐硬件配置
- **内存**: 16GB+ RAM (最低8GB)
- **硬盘**: 100GB+ 可用空间 (SSD推荐)
- **CPU**: 4核心+ (推荐8核心)

---

## 🛠️ 详细安装步骤

### 1. Java 17 安装

#### Windows
```bash
# 使用Chocolatey安装 (推荐)
choco install openjdk17

# 或手动下载安装
# 下载地址: https://adoptium.net/
# 选择 Temurin JDK 17 LTS 版本
```

#### macOS
```bash
# 使用Homebrew安装
brew install openjdk@17

# 设置环境变量
echo 'export PATH="/usr/local/opt/openjdk@17/bin:$PATH"' >> ~/.zshrc
```

#### Linux (Ubuntu)
```bash
# 更新包管理器
sudo apt update

# 安装OpenJDK 17
sudo apt install openjdk-17-jdk

# 验证安装
java -version
javac -version
```

**验证命令**:
```bash
java -version  # 应显示 openjdk version "17.x.x"
echo $JAVA_HOME # 应显示JDK安装路径
```

### 2. Maven 3.8+ 安装

#### Windows
```bash
# 使用Chocolatey安装
choco install maven

# 或手动下载
# 下载地址: https://maven.apache.org/download.cgi
# 解压到 C:\Program Files\Apache\maven
# 添加环境变量 MAVEN_HOME 和 PATH
```

#### macOS
```bash
# 使用Homebrew安装
brew install maven
```

#### Linux
```bash
# Ubuntu/Debian
sudo apt install maven

# 或手动安装
wget https://archive.apache.org/dist/maven/maven-3/3.8.6/binaries/apache-maven-3.8.6-bin.tar.gz
sudo tar -xzf apache-maven-3.8.6-bin.tar.gz -C /opt/
sudo ln -s /opt/apache-maven-3.8.6 /opt/maven
```

**验证配置**:
```bash
mvn -version  # 应显示 Apache Maven 3.8.x
```

**配置Maven镜像** (推荐):
```xml
<!-- ~/.m2/settings.xml -->
<settings>
  <mirrors>
    <mirror>
      <id>aliyun-central</id>
      <mirrorOf>central</mirrorOf>
      <name>Aliyun Central</name>
      <url>https://maven.aliyun.com/repository/central</url>
    </mirror>
  </mirrors>
</settings>
```

### 3. MySQL 8.0 安装

#### Windows
```bash
# 使用Chocolatey安装
choco install mysql

# 或下载官方安装包
# https://dev.mysql.com/downloads/mysql/
```

#### macOS
```bash
# 使用Homebrew安装
brew install mysql

# 启动MySQL服务
brew services start mysql

# 设置root密码
mysql_secure_installation
```

#### Linux
```bash
# Ubuntu/Debian
sudo apt update
sudo apt install mysql-server

# 启动服务
sudo systemctl start mysql
sudo systemctl enable mysql

# 安全配置
sudo mysql_secure_installation
```

**数据库配置**:
```sql
-- 登录MySQL
mysql -u root -p

-- 创建项目数据库
CREATE DATABASE ioe_dream DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 创建项目用户
CREATE USER 'ioe_dream'@'localhost' IDENTIFIED BY 'your_password';
GRANT ALL PRIVILEGES ON ioe_dream.* TO 'ioe_dream'@'localhost';
FLUSH PRIVILEGES;

-- 验证连接
USE ioe_dream;
SHOW TABLES;
```

### 4. Redis 6.0+ 安装

#### Windows
```bash
# 使用Chocolatey安装
choco install redis-64

# 或下载Windows版本
# https://github.com/microsoftarchive/redis/releases
```

#### macOS
```bash
# 使用Homebrew安装
brew install redis

# 启动Redis服务
brew services start redis
```

#### Linux
```bash
# Ubuntu/Debian
sudo apt update
sudo apt install redis-server

# 启动服务
sudo systemctl start redis
sudo systemctl enable redis
```

**验证Redis**:
```bash
# 测试连接
redis-cli ping
# 应返回 PONG
```

### 5. Node.js 18+ 安装 (前端开发)

#### Windows
```bash
# 使用Chocolatey安装
choco install nodejs

# 或下载官方安装包
# https://nodejs.org/
```

#### macOS
```bash
# 使用Homebrew安装
brew install node@18
```

#### Linux
```bash
# 使用NodeSource仓库
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs
```

**验证安装**:
```bash
node -version   # 应显示 v18.x.x
npm -version    # 应显示对应的npm版本
```

### 6. Git 安装

#### Windows
```bash
# 下载Git for Windows
# https://git-scm.com/download/win
```

#### macOS
```bash
# 使用Homebrew安装
brew install git
```

#### Linux
```bash
# Ubuntu/Debian
sudo apt install git
```

**Git配置**:
```bash
# 配置用户信息
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"

# 配置默认编辑器
git config --global core.editor "notepad"  # Windows
git config --global core.editor "vim"     # Linux/macOS
```

---

## 🚀 项目环境配置
## 📋 IOE-DREAM七微服务架构

**核心架构组成**:
- **Gateway Service (8080)**: API网关
- **Common Service (8088)**: 公共模块微服务
- **DeviceComm Service (8087)**: 设备通讯微服务
- **OA Service (8089)**: OA微服务
- **Access Service (8090)**: 门禁服务
- **Attendance Service (8091)**: 考勤服务
- **Video Service (8092)**: 视频服务
- **Consume Service (8094)**: 消费服务
- **Visitor Service (8095)**: 访客服务

**架构特点**:
- 基于Spring Boot 3.5.8 + Java 17
- 严格遵循企业级微服务规范
- 支持高并发、高可用、水平扩展

**技术栈标准**:
- **数据库**: MySQL 8.0 + Druid连接池
- **缓存**: Redis + Caffeine多级缓存
- **注册中心**: Nacos
- **配置中心**: Nacos Config
- **认证授权**: Sa-Token

## 🏗️ 四层架构规范

**标准架构模式**:
```
Controller (接口控制层)
    ↓
Service (核心业务层)
    ↓
Manager (流程管理层)
    ↓
DAO (数据访问层)
```

**层级职责**:
- **Controller层**: HTTP请求处理、参数验证、权限控制
- **Service层**: 核心业务逻辑、事务管理、业务规则验证
- **Manager层**: 复杂流程编排、多数据组装、第三方服务集成
- **DAO层**: 数据库CRUD操作、SQL查询实现、数据访问边界

**严格禁止跨层访问**: Controller不能直接调用Manager/DAO！
### 1. 克隆项目代码
## ⚠️ IOE-DREAM零容忍规则（强制执行）

**必须遵守的架构规则**:
- ✅ **必须使用 @Resource 注入依赖**
- ✅ **必须使用 @Mapper 注解** (禁止@Repository)
- ✅ **必须使用 Dao 后缀** (禁止Repository)
- ✅ **必须使用 @RestController 注解**
- ✅ **必须使用 @Valid 参数校验**
- ✅ **必须返回统一ResponseDTO格式**
- ✅ **必须遵循四层架构边界**

**严格禁止事项**:
- ❌ **禁止使用 @Autowired 注入**
- ❌ **禁止使用 @Repository 注解**
- ❌ **禁止使用 Repository 后缀命名**
- ❌ **禁止跨层访问**
- ❌ **禁止在Controller中包含业务逻辑**
- ❌ **禁止直接访问数据库**

**违规后果**: P0级问题，立即修复，禁止合并！
```bash
# 克隆项目仓库
git clone https://github.com/your-org/ioe-dream.git

# 进入项目目录
cd ioe-dream
```

### 2. 配置IDE (推荐IntelliJ IDEA)

#### 必需插件
- **Lombok**: 支持@Data注解
- **MyBatis**: SQL提示和映射
- **Spring Boot**: Spring框架支持
- **Git**: 版本控制集成

#### 代码格式化配置
```xml
<!-- .editorconfig -->
root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true

[*.java]
indent_style = space
indent_size = 4
```

### 3. 数据库初始化
```bash
# 导入数据库结构
mysql -u root -p ioe_dream < database/init.sql

# 导入基础数据
mysql -u root -p ioe_dream < database/data.sql
```

### 4. 配置文件设置
```yaml
# application-dev.yml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ioe_dream?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: ioe_dream
    password: your_password
    driver-class-name: com.mysql.cj.jdbc.Driver

  redis:
    host: localhost
    port: 6379
    database: 0
    timeout: 3000
```

---

## 🔧 开发工具配置

### 1. IDE环境变量设置

#### IntelliJ IDEA
```
File → Settings → Build Tools → Maven
Maven home path: [你的Maven安装路径]
User settings file: ~/.m2/settings.xml
Local repository: ~/.m2/repository
```

#### VS Code (可选)
```json
// .vscode/settings.json
{
    "java.home": "你的JDK安装路径",
    "java.configuration.updateBuildConfiguration": "automatic",
    "maven.executable.path": "你的Maven安装路径/bin/mvn"
}
```

### 2. 系统环境变量

#### Windows
```cmd
# 设置环境变量
setx JAVA_HOME "C:\Program Files\Java\jdk-17"
setx MAVEN_HOME "C:\Program Files\Apache\maven"
setx PATH "%PATH%;%JAVA_HOME%\bin;%MAVEN_HOME%\bin"

# 验证设置
echo %JAVA_HOME%
echo %MAVEN_HOME%
```

#### Linux/macOS
```bash
# 添加到 ~/.bashrc 或 ~/.zshrc
export JAVA_HOME=/usr/lib/jvm/java-17-openjdk
export MAVEN_HOME=/opt/maven
export PATH=$PATH:$JAVA_HOME/bin:$MAVEN_HOME/bin

# 重新加载配置
source ~/.bashrc  # 或 source ~/.zshrc
```

---

## 🧪 环境验证

### 1. 基础环境验证脚本
```bash
#!/bin/bash
# env-check.sh

echo "🔍 环境检查开始..."

# 检查Java
if command -v java &> /dev/null; then
    echo "✅ Java: $(java -version 2>&1 | head -n 1)"
else
    echo "❌ Java 未安装"
fi

# 检查Maven
if command -v mvn &> /dev/null; then
    echo "✅ Maven: $(mvn -version 2>&1 | head -n 1)"
else
    echo "❌ Maven 未安装"
fi

# 检查MySQL
if command -v mysql &> /dev/null; then
    echo "✅ MySQL: $(mysql --version)"
else
    echo "❌ MySQL 未安装"
fi

# 检查Redis
if command -v redis-cli &> /dev/null; then
    echo "✅ Redis: $(redis-cli --version)"
else
    echo "❌ Redis 未安装"
fi

# 检查Node.js
if command -v node &> /dev/null; then
    echo "✅ Node.js: $(node --version)"
else
    echo "❌ Node.js 未安装 (前端开发可选)"
fi

echo "🔍 环境检查完成"
```

### 2. 项目启动验证
```bash
# 进入项目根目录
cd ioe-dream

# 编译项目
mvn clean compile -DskipTests

# 启动Nacos (如果需要)
# cd nacos/bin
# ./startup.sh

# 启动应用
mvn spring-boot:run

# 验证应用是否启动成功
curl http://localhost:8080/actuator/health
```

---

## 🚨 常见问题解决

### 1. Java版本问题
**问题**: `java: package javax.annotation does not exist`
**解决**: 确保使用Java 17，并在IDE中设置正确的JDK版本

### 2. Maven依赖下载慢
**问题**: Maven依赖下载缓慢
**解决**: 配置阿里云镜像 (见上文Maven配置)

### 3. MySQL连接失败
**问题**: `Communications link failure`
**解决**:
- 检查MySQL服务是否启动
- 验证数据库用户名密码
- 检查防火墙设置

### 4. Redis连接超时
**问题**: `Redis connection timeout`
**解决**:
- 检查Redis服务是否启动
- 验证Redis配置文件
- 检查网络连接

### 5. 端口占用
**问题**: `Port 8080 already in use`
**解决**:
```bash
# 查找占用端口的进程
netstat -tulpn | grep 8080  # Linux
netstat -ano | findstr 8080  # Windows

# 终止进程
kill -9 <PID>  # Linux
taskkill /PID <PID> /F  # Windows
```

---

## 📚 后续步骤

### 1. 完成环境搭建后
1. **阅读项目架构文档**: [全局架构规范](../../01-核心规范/架构规范/全局架构规范.md)
2. **学习编码规范**: [Java编码规范](../../01-核心规范/开发规范/Java编码规范.md)
3. **运行示例项目**: 按照快速开始指南操作

### 2. 加入开发团队
1. **配置Git SSH密钥**: 生成并添加SSH公钥到代码仓库
2. **设置IDE配置**: 导入项目代码和配置文件
3. **加入团队沟通**: 加入开发团队群组和邮件列表

### 3. 开始开发
1. **创建开发分支**: `git checkout -b feature/your-feature`
2. **编写代码**: 遵循项目编码规范
3. **提交代码**: 使用规范的提交信息
4. **创建Pull Request**: 提交代码审查

---

## 📞 技术支持

### 获取帮助
- **环境问题**: 联系DevOps团队
- **项目问题**: 在团队群中求助
- **文档问题**: 提交Git Issue

### 有用的链接
- [Java官方文档](https://docs.oracle.com/en/java/)
- [Maven官方文档](https://maven.apache.org/guides/)
- [MySQL官方文档](https://dev.mysql.com/doc/)
- [Redis官方文档](https://redis.io/documentation)

---

## 📝 版本信息

**文档版本**: v2.0.0
**最后更新**: 2025-12-02
**维护团队**: IOE-DREAM DevOps团队
**适用项目**: IOE-DREAM智慧园区管理平台

---

**🎯 环境搭建成功后，您就可以开始IOE-DREAM项目的开发了！祝您编码愉快！**