# 系统配置管理 - 详细设计说明书

> **版本**: v1.0.0  
> **创建日期**: 2025-12-17

---

## 📁 配置类结构

```
net.lab1024.sa.access/
├── config/
│   ├── AccessMetricsConfiguration.java
│   ├── AccessSecurityConfiguration.java
│   ├── AntiPassbackConfiguration.java
│   ├── CacheOptimizationConfiguration.java
│   ├── DatabaseOptimizationConfiguration.java
│   └── ManagerConfiguration.java
```

---

## 📊 配置项详细设计

### 核心配置项

| 配置键 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| access.heartbeat.interval | Integer | 30 | 心跳间隔(秒) |
| access.offline.threshold | Integer | 300 | 离线阈值(秒) |
| access.auth.timeout | Integer | 10 | 认证超时(秒) |
| access.log.retention | Integer | 365 | 日志保留(天) |
| access.cache.ttl | Integer | 3600 | 缓存过期(秒) |

---

## 🔌 Nacos配置示例

```yaml
# access-service.yml
access:
  heartbeat:
    interval: 30
    timeout: 90
  auth:
    timeout: 10
    maxRetry: 3
  cache:
    enabled: true
    ttl: 3600
  security:
    antiPassbackEnabled: true
    multiFactorRequired: false
```

---

## 📊 数据库设计

```sql
CREATE TABLE t_system_config (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  config_key VARCHAR(100) UNIQUE NOT NULL,
  config_value VARCHAR(500),
  config_type VARCHAR(30),
  description VARCHAR(200),
  create_time DATETIME NOT NULL,
  update_time DATETIME NOT NULL,
  INDEX idx_type(config_type)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE t_backup_record (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  backup_type VARCHAR(30) NOT NULL,
  file_path VARCHAR(500) NOT NULL,
  file_size BIGINT,
  status TINYINT DEFAULT 0,
  operator_id BIGINT NOT NULL,
  backup_time DATETIME NOT NULL,
  INDEX idx_time(backup_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

**📝 文档维护**: IOE-DREAM架构团队 | 2025-12-17
