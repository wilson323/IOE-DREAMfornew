# 审批流程管理 - 详细设计说明书

> **版本**: v1.0.0  
> **创建日期**: 2025-12-17

---

## 📁 类结构设计

```
net.lab1024.sa.access/
├── controller/
│   ├── AccessPermissionApplyController.java
│   └── AccessEmergencyPermissionController.java
├── service/
│   ├── AccessPermissionApplyService.java
│   ├── AccessEmergencyPermissionService.java
│   └── impl/
│       ├── AccessPermissionApplyServiceImpl.java
│       └── AccessEmergencyPermissionServiceImpl.java
└── dao/
    └── AccessPermissionApplyDao.java
```

---

## 🔌 接口详细设计

### POST /api/access/v1/permission/apply

**请求参数**:
```json
{
  "targetAreaIds": [1001, 1002],
  "startTime": "2025-12-20T09:00:00Z",
  "endTime": "2025-12-20T18:00:00Z",
  "reason": "项目对接需要进入数据中心"
}
```

**响应数据**:
```json
{
  "code": 200,
  "data": {
    "applyId": 1001,
    "applyNo": "APR-202512-0001",
    "status": "PENDING",
    "currentApprover": "王经理"
  }
}
```

---

## 📊 数据库设计

```sql
CREATE TABLE t_permission_apply (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  apply_no VARCHAR(30) UNIQUE NOT NULL,
  applicant_id BIGINT NOT NULL,
  apply_type VARCHAR(30) NOT NULL DEFAULT 'NORMAL',
  target_areas JSON,
  start_time DATETIME,
  end_time DATETIME,
  reason VARCHAR(500),
  status TINYINT DEFAULT 0,
  current_approver BIGINT,
  create_time DATETIME NOT NULL,
  update_time DATETIME NOT NULL,
  INDEX idx_applicant(applicant_id),
  INDEX idx_approver(current_approver),
  INDEX idx_status(status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

**📝 文档维护**: IOE-DREAM架构团队 | 2025-12-17
