# IOE-DREAM 统一开发规范（权威版本）

> **📋 版本**: v6.0.0 (双轨制架构版)
> **📅 创建时间**: 2025-11-14
> **📅 最后更新**: 2025-11-27
> **📋 文档职责**: 统一所有IOE-DREAM开发规范，支持双轨制架构（单体 + 微服务）
> **📋 适用范围**: IOE-DREAM智能管理系统 所有开发工作（单体架构 + 微服务架构）

---

## 🚨 **重要说明：本规范为唯一权威来源**

### ⚠️ **规范冲突解决原则**
1. **本规范优先级最高**：如有冲突，以本规范为准
2. **其他规范文档逐步统一**：所有旧版本文档需要按本规范更新
3. **实施时间**：本文档发布后立即生效

### 📚 **被统一的文档清单**
- CLAUDE.md → 技术栈规范部分
- DEV_STANDARDS.md → 技术标准部分
- TECHNOLOGY_MIGRATION.md → 迁移规范部分
- IOE-DREAM统一开发规范技能指南.md → 技能指南部分
- API规范.md → API设计部分
- 所有CHECKLISTS文档 → 检查清单部分

---

## 🔧 **技术栈规范（必须使用）**

### 🖥️ 后端技术栈

#### 单体架构技术栈
```markdown
✅ 强制要求：
- 框架版本：Spring Boot 3.5.7
- 开发语言：Java 17 (LTS)
- ORM框架：MyBatis Plus 3.5.7
- 安全框架：Sa-Token 1.37.0
- 数据库：MySQL 8.0+
- 缓存：Redis 6.0+
- 连接池：Druid 1.2.21
- JSON处理：Fastjson 2.0.57
- 构建工具：Maven 3.9+
- 日志框架：Log4j2 + @Slf4j

❌ 绝对禁止：
- 使用Spring Boot 2.x版本
- 业务代码中使用javax.* 包（必须使用jakarta.*）
- 使用@Autowired（必须使用@Resource）
- 使用System.out.println()
```

#### 微服务架构技术栈
```markdown
✅ 强制要求：
- 框架版本：Spring Boot 3.5.7 + Spring Cloud 2023.0.3
- 开发语言：Java 17 (LTS)
- ORM框架：MyBatis Plus 3.5.7
- 服务注册发现：Nacos 2.3+
- 配置中心：Nacos Config
- 服务调用：OpenFeign 4.1
- 熔断降级：Sentinel 1.8.6
- 负载均衡：Spring Cloud LoadBalancer
- 链路追踪：Micrometer Tracing
- 安全框架：Sa-Token 1.37.0
- 数据库：MySQL 8.0+ （每个服务独立数据库）
- 缓存：Redis 6.0+ （集群模式）
- 消息队列：RabbitMQ 3.12+
- 网关：Spring Cloud Gateway
- 构建工具：Maven 3.9+
- 日志框架：Log4j2 + @Slf4j

❌ 绝对禁止：
- 服务间直接数据库访问（必须通过API）
- 跨服务事务（使用分布式事务解决方案）
- 硬编码服务地址（必须使用服务发现）
- 使用@Autowired（必须使用@Resource）
- 使用System.out.println()

⚠️ **包名管理严格分层策略（基于repowiki权威标准）：**

**🟢 Spring Boot 3.x jakarta包使用（必须遵守）：**
- ✅ `jakarta.annotation.Resource` - 依赖注入（强制使用，禁止@Autowired）
- ✅ `jakarta.validation.*` - 验证注解
- ✅ `jakarta.persistence.*` - JPA注解
- ✅ `jakarta.servlet.*` - Servlet相关
- ✅ `jakarta.inject.*` - 依赖注入
- ✅ 所有Spring Boot 3原生jakarta包

**🟡 javax包允许使用场景（例外情况）：**
- ✅ `javax.sql.DataSource` - 数据源配置（Druid等第三方库强制要求）
- ✅ 第三方库强制要求的javax包（需在注释中说明原因）

**🔴 严格禁止的javax使用：**
- ❌ `javax.annotation.Resource` → 必须改为 `jakarta.annotation.Resource`
- ❌ `javax.validation.*` → 必须改为 `jakarta.validation.*`
- ❌ `javax.persistence.*` → 必须改为 `jakarta.persistence.*`
- ❌ `javax.servlet.*` → 必须改为 `jakarta.servlet.*`
```

### 🌐 前端技术栈
```markdown
✅ 强制要求：
- 框架：Vue 3.4.27 + Composition API
- UI组件库：Ant Design Vue 4.2.5
- 状态管理：Pinia 2.1.7
- 构建工具：Vite 5.2.12
- HTTP客户端：Axios 1.6.8
- 路由管理：Vue Router 4.x
- 类型检查：TypeScript 5.0+ (推荐严格模式)

❌ 绝对禁止：
- 使用Vue 2.x语法
- 直接修改props
- 使用var声明变量
- 使用any类型（除非必要）
```

---

## 📁 **项目结构规范（双轨制标准）**

### 🏗️ 单体架构项目结构（生产稳定版）
```
smart-admin-api-java17-springboot3/
├── sa-base/                           # 基础模块
│   ├── src/main/java/net/lab1024/sa/
│   │   ├── common/                   # 公共模块（统一使用common）
│   │   │   ├── domain/               # Entity, VO, DTO
│   │   │   │   ├── entity/           # 数据库实体
│   │   │   │   ├── vo/               # 视图对象
│   │   │   │   ├── dto/              # 数据传输对象
│   │   │   │   └── form/             # 表单对象
│   │   │   ├── config/               # 配置类
│   │   │   ├── util/                 # 工具类
│   │   │   ├── constant/             # 常量定义
│   │   │   └── exception/            # 异常定义
│   │   └── module/                  # 核心功能模块
│   │       ├── cache/               # 缓存模块
│   │       ├── websocket/           # WebSocket模块
│   │       └── support/            # 支持模块
│   └── src/main/resources/
│       ├── dev/sa-base.yaml         # 开发环境配置
│       ├── test/sa-base.yaml        # 测试环境配置
│       └── prod/sa-base.yaml        # 生产环境配置
└── sa-admin/                         # 管理模块
    └── src/main/java/net/lab1024/sa/
        ├── admin/                     # 业务代码
        │   ├── controller/           # 控制器层
        │   ├── service/              # 服务层
        │   ├── manager/              # 管理层
        │   └── dao/                  # 数据访问层
        └── module/                    # 业务模块
            ├── system/               # 系统模块
            ├── business/            # 业务模块
            └── oa/                   # 办公模块
```

### 🏗️ 微服务架构项目结构（未来目标版）
```
microservices/                         # 微服务根目录
├── microservices-common/              # 公共模块（独立Maven模块）
│   ├── src/main/java/net/lab1024/sa/common/
│   │   ├── domain/                   # 共享领域对象
│   │   ├── config/                   # 公共配置
│   │   ├── util/                     # 工具类
│   │   ├── constant/                 # 常量定义
│   │   ├── exception/                # 异常定义
│   │   └── feign/                    # Feign客户端基类
│   └── pom.xml
├── smart-gateway/                     # 智能网关
│   ├── src/main/java/net/lab1024/sa/gateway/
│   │   ├── GatewayApplication.java
│   │   ├── config/                   # 网关配置
│   │   ├── filter/                   # 网关过滤器
│   │   └── fallback/                 # 熔断降级
│   └── src/main/resources/
│       └── application.yml
├── ioedream-auth-service/             # 认证服务（8081）
│   ├── src/main/java/net/lab1024/sa/auth/
│   │   ├── AuthApplication.java
│   │   ├── controller/               # 认证控制器
│   │   ├── service/                  # 认证服务
│   │   ├── manager/                  # 认证管理
│   │   ├── dao/                      # 数据访问
│   │   ├── domain/entity/            # 用户实体
│   │   ├── domain/vo/                # 认证VO
│   │   ├── domain/dto/               # 认证DTO
│   │   ├── feign/                    # Feign客户端
│   │   └── config/                   # 服务配置
│   └── src/main/resources/
│       ├── application.yml            # 服务配置
│       └── bootstrap.yml             # 引导配置
├── ioedream-identity-service/         # 身份权限服务（8082）
├── ioedream-device-service/           # 设备管理服务（8083）
├── ioedream-area-service/             # 区域管理服务（8084）
├── ioedream-access-service/           # 门禁服务（8085）
├── ioedream-consume-service/          # 消费服务（8086）
├── ioedream-attendance-service/       # 考勤服务（8087）
├── ioedream-video-service/            # 视频服务（8088）
├── ioedream-visitor-service/         # 访客服务（8089）✅完整实现
├── ioedream-notification-service/     # 通知服务（8090）
├── ioedream-file-service/             # 文件服务（8091）
├── ioedream-report-service/           # 报表服务（8092）
├── docker/                           # Docker配置
│   ├── infrastructure.yml            # 基础设施
│   ├── basic-services.yml            # 基础服务
│   ├── business-services.yml         # 业务服务
│   └── monitoring.yml                # 监控服务
├── nacos/                            # Nacos配置
│   └── config/                       # 配置文件
└── pom.xml                           # 父POM
```

### 🏗️ 后端项目结构
```
smart-admin-api-java17-springboot3/
├── sa-base/                           # 基础模块
│   ├── src/main/java/net/lab1024/sa/
│   │   ├── common/                   # 公共模块（统一使用common）
│   │   │   ├── domain/               # Entity, VO, DTO
│   │   │   │   ├── entity/           # 数据库实体
│   │   │   │   ├── vo/               # 视图对象
│   │   │   │   ├── dto/              # 数据传输对象
│   │   │   │   └── form/             # 表单对象
│   │   │   ├── config/               # 配置类
│   │   │   ├── util/                 # 工具类
│   │   │   ├── constant/             # 常量定义
│   │   │   └── exception/            # 异常定义
│   │   └── module/                  # 核心功能模块
│   │       ├── cache/               # 缓存模块
│   │       ├── websocket/           # WebSocket模块
│   │       └── support/            # 支持模块
│   └── src/main/resources/
│       ├── dev/sa-base.yaml         # 开发环境配置
│       ├── test/sa-base.yaml        # 测试环境配置
│       └── prod/sa-base.yaml        # 生产环境配置
└── sa-admin/                         # 管理模块
    └── src/main/java/net/lab1024/sa/
        ├── admin/                     # 业务代码
        │   ├── controller/           # 控制器层
        │   ├── service/              # 服务层
        │   ├── manager/              # 管理层
        │   └── dao/                  # 数据访问层
        └── module/                    # 业务模块
            ├── system/               # 系统模块
            ├── business/            # 业务模块
            └── oa/                   # 办公模块
```

### 🎨 前端项目结构
```
smart-admin-web-javascript/
├── src/
│   ├── api/                          # API接口
│   ├── assets/                       # 静态资源
│   ├── components/                   # 公共组件
│   ├── config/                       # 配置文件
│   ├── constants/                    # 常量定义
│   ├── directives/                   # 自定义指令
│   ├── i18n/                         # 国际化
│   ├── layout/                       # 布局组件
│   ├── lib/                          # 第三方库扩展
│   ├── plugins/                      # 插件
│   ├── router/                       # 路由配置
│   ├── store/                        # Pinia状态管理
│   ├── theme/                        # 主题配置
│   ├── utils/                        # 工具函数
│   ├── views/                        # 页面组件
│   ├── App.vue                       # 根组件
│   └── main.js                       # 入口文件
├── vite.config.js                    # Vite配置
└── package.json
```

---

## 🚫 **绝对禁止清单（不可违反）**

### 🔴 **后端开发禁止项**
```markdown
❌ 禁止使用 @Autowired，必须使用 @Resource
❌ 禁止使用 javax.* 包，必须使用 jakarta.*
❌ 禁止使用 System.out.println()，必须使用日志框架
❌ 禁止跨层直接访问（如Controller直接访问Dao）
❌ 禁止在Controller中编写业务逻辑
❌ 禁止使用魔法数字，必须定义为常量
❌ 禁止硬编码配置信息
❌ 禁止忽略异常处理
❌ 禁止代码复杂度超过10
❌ 禁止重复代码率超过3%
```

### 🔴 **前端开发禁止项**
```markdown
❌ 禁止使用过时的Vue 2语法
❌ 禁止直接修改props
❌ 禁止在模板中使用复杂表达式
❌ 禁止使用全局CSS污染组件
❌ 禁止缺少必要的TypeScript类型注解
❌ 禁止使用undefined和null不一致
❌ 禁止滥用v-for的key属性
❌ 禁止在computed中产生副作用
```

### 🔴 **数据库设计禁止项**
```markdown
❌ 禁止使用物理删除，必须使用软删除
❌ 禁止缺少必要审计字段
❌ 禁止使用拼音或不规范命名
❌ 禁止缺少主键和索引设计
❌ 禁止单表超过1000万行不进行分表
❌ 禁止SELECT * 查询
❌ 禁止在WHERE子句中使用函数
```

---

## ✅ **必须执行标准（强制）**

### 🏗️ **架构设计标准**
```markdown
✅ 严格遵循四层架构：Controller → Service → Manager → DAO
✅ 事务边界必须在Service层，使用@Transactional注解
✅ 所有接口必须进行权限验证
✅ 必须使用统一响应格式ResponseDTO
✅ 必须遵循RESTful API设计规范
✅ 必须实现完整的异常处理机制
✅ 必须编写单元测试，覆盖率≥80%
✅ 必须遵循依赖注入规范
```

### 🔧 **代码质量标准**
```markdown
✅ 代码复杂度 ≤ 10
✅ 重复代码率 ≤ 3%
✅ 单元测试覆盖率 ≥ 80%
✅ 代码审查通过率 = 100%
✅ 规范检查通过率 = 100%
✅ 安全扫描通过率 = 100%
```

### 📝 **文档标准**
```markdown
✅ 所有public方法必须有JavaDoc注释
✅ 复杂业务逻辑必须有注释说明
✅ 接口必须提供示例文档
✅ 数据库表必须有设计文档
✅ 关键算法必须有设计说明
```

---

## 🎯 **规范执行机制**

### 🔍 **自动化检查工具**
```bash
# 强制规范检查脚本
./scripts/enforce-standards.sh

# 每次提交前必须执行
pre-commit hook → enforce-standards.sh

# CI/CD流水线质量门禁
编译检查 → 规范检查 → 测试检查 → 安全扫描
```

### 📊 **质量度量指标**
```markdown
✅ 编译成功率：100%
✅ 规范违规率：0%
✅ 代码覆盖率：≥80%
✅ 技术债务密度：<5%
✅ 循环依赖数：0
✅ 安全漏洞数：0
```

### ⚖️ **违规后果机制**
```markdown
🔴 一级规范违规：
- 阻塞开发，立即修复
- CI/CD流水线失败
- 代码审查不通过

🟡 二级规范违规：
- 技术债务记录
- 限期修复
- 性能影响评估

🟢 三级规范违规：
- 代码质量建议
- 最佳实践推荐
- 择机优化
```

---

## 📋 **开发检查清单**

### 📝 **开发前检查**
- [ ] 阅读对应模块设计文档
- [ ] 理解业务需求和约束条件
- [ ] 确认技术栈版本
- [ ] 准备开发环境

### 💻 **编码过程中检查**
- [ ] 遵循命名规范
- [ ] 使用正确依赖注入
- [ ] 实现异常处理
- [ ] 编写必要注释
- [ ] 遵循架构分层

### ✅ **提交前检查**
- [ ] 代码编译通过
- [ ] 单元测试通过
- [ ] 规范检查通过
- [ ] 代码审查通过
- [ ] 功能验证完成

### 🚀 **部署前检查**
- [ ] 集成测试通过
- [ ] 性能测试达标
- [ ] 安全扫描通过
- [ ] 部署文档完整
- [ ] 监控配置完成

---

## 🔧 **开发工具配置**

### 💻 **IDE配置**
```xml
<!-- IntelliJ IDEA 配置 -->
<component name="ProjectCodeStyleConfiguration">
  <code_scheme name="SmartAdmin" version="173">
    <JavaCodeStyleSettings>
      <option name="IMPORT_LAYOUT_TABLE">
        <value>
          <package name="jakarta" withSubpackages="true" static="false"/>
          <package name="javax" withSubpackages="false" static="false"/>
          <package name="org.springframework" withSubpackages="true" static="false"/>
        </value>
      </option>
    </JavaCodeStyleSettings>
  </code_scheme>
</component>
```

### 🔍 **代码检查配置**
```xml
<!-- Checkstyle 配置 -->
<module name="Checker">
  <module name="TreeWalker">
    <module name="MethodLength">
      <property name="max" value="30"/>
    </module>
    <module name="ParameterNumber">
      <property name="max" value="10"/>
    </module>
    <module name="CyclomaticComplexity">
      <property name="max" value="10"/>
    </module>
  </module>
</module>
```

---

## 📚 **附录：规范更新日志**

### v5.0.0 (2025-11-14)
- ✅ 统一所有规范文档，消除冲突
- ✅ 更新技术栈版本要求
- ✅ 完善规范执行机制
- ✅ 添加详细的质量度量标准

### v4.0.0 (历史版本)
- 各规范文档分别制定，存在不一致问题

---

**维护责任**：SmartAdmin技术委员会
**更新频率**：每季度评估，必要时更新
**适用范围**：SmartAdmin项目所有开发团队
**生效时间**：本文档发布后立即生效