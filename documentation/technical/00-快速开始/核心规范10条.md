# IOE-DREAM 核心规范10条

> **零容忍政策**: 违反任何一条都将导致代码被拒绝合并

---

## 🔴 第1条：包名规范 - 零容忍

```java
❌ 禁止: import javax.servlet.*;
❌ 禁止: import javax.validation.*;
❌ 禁止: import javax.annotation.*;

✅ 必须: import jakarta.servlet.*;
✅ 必须: import jakarta.validation.*;
✅ 必须: import jakarta.annotation.*;
```

**原因**: Spring Boot 3.x 使用 Jakarta EE 9+

**检查命令**:
```bash
find . -name "*.java" -exec grep -l "javax\." {} \;  # 必须返回0
```

---

## 🔴 第2条：依赖注入 - 零容忍

```java
❌ 禁止: @Autowired
private UserService userService;

✅ 必须: @Resource
private UserService userService;
```

**原因**: 统一依赖注入方式，提高代码可读性

**检查命令**:
```bash
find . -name "*.java" -exec grep -l "@Autowired" {} \;  # 必须返回0
```

---

## 🔴 第3条：日志输出 - 零容忍

```java
❌ 禁止: System.out.println("调试信息");
❌ 禁止: System.err.println("错误信息");

✅ 必须: @Slf4j
log.info("操作信息: {}", data);
log.error("错误信息: {}", error);
```

**原因**: 统一日志管理，支持日志级别控制

**检查命令**:
```bash
find . -name "*.java" -exec grep -l "System\.out\." {} \;  # 必须返回0
```

---

## 🔴 第4条：四层架构 - 零容忍

```java
❌ 禁止: Controller直接访问DAO
@RestController
public class UserController {
    @Resource
    private UserDao userDao;  // ❌ 错误
}

✅ 必须: 严格的四层调用链
Controller → Service → Manager → DAO
```

**原因**: 保证架构清晰，职责分离

**检查命令**:
```bash
grep -r "@Resource.*Dao" --include="*Controller.java" .  # 必须返回0
```

---

## 🔴 第5条：实体类设计 - 零容忍

```java
❌ 禁止: 重复定义审计字段
@Entity
public class UserEntity {
    private Long id;
    private LocalDateTime createTime;  // ❌ BaseEntity已包含
    private LocalDateTime updateTime;  // ❌ BaseEntity已包含
}

✅ 必须: 继承BaseEntity，不重复定义
@Entity
public class UserEntity extends BaseEntity {
    private Long id;
    private String username;
    // 不需要定义createTime, updateTime等审计字段
}
```

**原因**: 避免字段重复，确保审计字段一致性

---

## 🔴 第6条：权限控制 - 强制要求

```java
❌ 禁止: 接口没有权限控制
@GetMapping("/users")
public ResponseDTO<List<UserVO>> getUsers() {  // ❌ 缺少权限注解
    return ResponseDTO.ok(userService.getUsers());
}

✅ 必须: 每个接口都要有权限注解
@GetMapping("/users")
@SaCheckPermission("user:list")  // ✅ 必须添加
public ResponseDTO<List<UserVO>> getUsers() {
    return ResponseDTO.ok(userService.getUsers());
}
```

**原因**: 确保系统安全性，防止未授权访问

---

## 🔴 第7条：参数验证 - 强制要求

```java
❌ 禁止: 接口参数不验证
@PostMapping("/create")
public ResponseDTO<Long> createUser(@RequestBody UserCreateDTO dto) {  // ❌ 缺少@Valid
    return ResponseDTO.ok(userService.createUser(dto));
}

✅ 必须: 使用@Valid进行参数验证
@PostMapping("/create")
public ResponseDTO<Long> createUser(@Valid @RequestBody UserCreateDTO dto) {  // ✅ 添加@Valid
    return ResponseDTO.ok(userService.createUser(dto));
}
```

**原因**: 确保数据完整性，防止非法数据

---

## 🔴 第8条：事务管理 - 强制要求

```java
❌ 禁止: 在Controller层管理事务
@RestController
public class UserController {
    @Transactional  // ❌ 事务边界错误
    public ResponseDTO<Long> createUser(UserCreateDTO dto) {
        // 业务逻辑
    }
}

✅ 必须: 在Service层管理事务
@Service
public class UserService {
    @Transactional  // ✅ 正确的事务边界
    public Long createUser(UserCreateDTO dto) {
        // 业务逻辑
    }
}
```

**原因**: 保证事务边界清晰，避免长事务

---

## 🔴 第9条：异常处理 - 强制要求

```java
❌ 禁止: 直接抛出或捕获Exception
try {
    // 业务逻辑
} catch (Exception e) {  // ❌ 过于宽泛
    log.error("操作失败", e);
    throw new RuntimeException("操作失败");
}

✅ 必须: 使用具体异常类型和SmartException
try {
    // 业务逻辑
} catch (UserNotFoundException e) {  // ✅ 具体异常类型
    log.error("用户不存在: {}", e.getMessage());
    throw SmartException.get("用户不存在");
}
```

**原因**: 精确的异常处理，便于问题定位

---

## 🔴 第10条：编码规范 - 强制要求

```java
❌ 禁止: 不规范的命名
public class userController {  // ❌ 类名应该大写
    private UserService userService;  // ❌ 变量名不规范

    public List<userVO> getuserlist() {  // ❌ 方法名不规范
        // 逻辑
    }
}

✅ 必须: 遵循Java命名规范
public class UserController {  // ✅ 类名大驼峰
    private UserService userService;  // ✅ 变量名小驼峰

    public List<UserVO> getUserList() {  // ✅ 方法名小驼峰
        // 逻辑
    }
}
```

**原因**: 提高代码可读性和维护性

---

## 🔍 快速自检脚本

在提交代码前，运行以下命令确保零违规：

```bash
#!/bin/bash
echo "🔍 检查核心规范10条..."

# 1. 包名检查
javax_count=$(find . -name "*.java" -exec grep -l "javax\." {} \; | wc -l)
if [ $javax_count -ne 0 ]; then
    echo "❌ 第1条违规: 发现 $javax_count 个文件使用javax包"
    exit 1
fi

# 2. 依赖注入检查
autowired_count=$(find . -name "*.java" -exec grep -l "@Autowired" {} \; | wc -l)
if [ $autowired_count -ne 0 ]; then
    echo "❌ 第2条违规: 发现 $autowired_count 个文件使用@Autowired"
    exit 1
fi

# 3. 日志检查
system_out_count=$(find . -name "*.java" -exec grep -l "System\.out\." {} \; | wc -l)
if [ $system_out_count -ne 0 ]; then
    echo "❌ 第3条违规: 发现 $system_out_count 个文件使用System.out"
    exit 1
fi

# 4. 架构违规检查
architecture_violations=$(grep -r "@Resource.*Dao" --include="*Controller.java" . | wc -l)
if [ $architecture_violations -ne 0 ]; then
    echo "❌ 第4条违规: 发现 $architecture_violations 处Controller直接访问DAO"
    exit 1
fi

echo "✅ 核心规范10条检查通过！"
```

---

## 🚨 违规后果

- **编译错误**: 直接无法运行
- **代码审查**: 被拒绝合并
- **CI/CD**: 流水线失败
- **绩效影响**: 影响个人评价

---

## 💡 最佳实践

### 开发时检查
```bash
# 实时检查（IDE集成推荐）
mvn clean compile -q  # 每次保存后检查

# 完整检查
./scripts/enforce-standards.sh
```

### 提交前验证
```bash
# 必须执行的检查
./scripts/commit-guard.sh
./scripts/ai-code-validation.sh
```

### 团队协作
- 代码审查时重点检查这10条
- 新人入职时必须学习这10条
- 定期进行规范培训

---

## 📚 相关文档

- **[详细规范文档](../../CLAUDE.md)** - 完整项目规范
- **[repowiki规范体系](../repowiki/zh/content/开发规范体系.md)** - 权威规范
- **[代码模板库](../repowiki/zh/content/开发规范体系/代码模板库.md)** - 标准代码模板
- **[技能体系](../../.claude/skills/README.md)** - AI辅助技能

---

**记住**: 这10条规范是项目质量的基石，必须100%遵守！

**更新时间**: 2025-11-21
**执行方式**: 零容忍，任何违规都将被拒绝