# 消费管理模块 - 快速测试说明

## 🚀 方式一：直接 URL 访问（推荐用于开发测试）

### 步骤 1：启用静态路由

在 `src/router/routers.js` 文件中添加：

```javascript
import { consumptionRouters } from './business/consumption';

export const routerArray = [
    ...loginRouters,
    ...homeRouters, 
    ...helpDocRouters,
    ...consumptionRouters,  // ⭐️ 添加这一行
    { path: '/:pathMatch(.*)*', name: '404', component: NotFound },
    { path: '/403', name: '403', component: NoPrivilege }
];
```

### 步骤 2：启动项目

```bash
npm run dev
```

### 步骤 3：访问页面

在浏览器中打开：

```
http://localhost:5173/#/business/consumption/dashboard
```

---

## 🎯 方式二：从首页跳转（推荐用于生产环境）

### 临时测试方法

1. 修改首页 (`src/views/system/home/index.vue`)，添加一个测试按钮：

```vue
<template>
  <a-button type="primary" @click="goToConsumption">
    <DashboardOutlined />
    消费管理Dashboard
  </a-button>
</template>

<script setup>
import { useRouter } from 'vue-router';
import { DashboardOutlined } from '@ant-design/icons-vue';

const router = useRouter();

function goToConsumption() {
  router.push('/business/consumption/dashboard');
}
</script>
```

2. 点击按钮即可跳转

---

## 📊 当前页面功能清单

### ✅ 已实现功能

- [x] Hero Section 欢迎区域
- [x] 系统通知组件（支持 info/success/warning/error）
- [x] 4个统计卡片
  - [x] 今日营业额
  - [x] 今日订单数
  - [x] 活跃用户
  - [x] 平均客单价
- [x] 趋势指示器（上升/下降）
- [x] 快速操作入口（9个功能卡片）
- [x] 最近活动列表
- [x] 刷新功能
- [x] 加载状态
- [x] 响应式布局
- [x] 模拟数据（带随机变化）

### 🔄 模拟数据说明

当前页面使用前端模拟数据：

```javascript
// 统计数据每次刷新会随机变化
{
  turnover: '¥10,000 ~ ¥30,000',
  orderCount: '500 ~ 1000',
  activeUsers: '300 ~ 600',
  averagePrice: '¥10 ~ ¥20'
}

// 活动列表固定6条
activities: [
  '新用户注册',
  '批量补贴发放',
  '设备离线提醒',
  '数据同步完成',
  '订单高峰期',
  '月度对账完成'
]
```

### 📝 后端接口对接位置

所有需要对接的地方都有 `TODO` 标记：

```javascript
// 文件：src/views/business/consumption/dashboard/index.vue

// TODO: 对接后端接口
async function queryDashboardData() {
  // TODO: 后端接口对接
  // const res = await consumptionApi.queryDashboardStats();
  // if (res.data) {
  //   updateStatsData(res.data);
  // }
  
  // 模拟数据（开发阶段使用）
  // ... 当前的模拟代码
}

async function queryActivities() {
  // TODO: 后端接口对接
  // const res = await consumptionApi.queryRecentActivities({ pageSize: 10 });
  // if (res.data && res.data.list) {
  //   activities.value = res.data.list;
  //   return;
  // }
  
  // 模拟数据（开发阶段使用）
  // ... 当前的模拟代码
}
```

---

## 🔗 后端接口格式参考

### 1. 统计数据接口

**请求：** `GET /consumption/dashboard/stats`

**响应格式：**

```json
{
  "code": 1,
  "ok": true,
  "msg": "操作成功",
  "data": {
    "turnover": {
      "value": "¥12,580.00",
      "change": "+12.5%",
      "changeType": "positive"
    },
    "orderCount": {
      "value": "856",
      "change": "+8.3%",
      "changeType": "positive"
    },
    "activeUsers": {
      "value": "425",
      "change": "+15.2%",
      "changeType": "positive"
    },
    "averagePrice": {
      "value": "¥14.70",
      "change": "-2.1%",
      "changeType": "negative"
    }
  }
}
```

### 2. 活动列表接口

**请求：** `POST /consumption/dashboard/activities`

**请求参数：**

```json
{
  "pageSize": 10
}
```

**响应格式：**

```json
{
  "code": 1,
  "ok": true,
  "msg": "操作成功",
  "data": {
    "list": [
      {
        "id": 1,
        "type": "success",
        "icon": "UserAddOutlined",
        "title": "新用户注册",
        "description": "张三 (工号: EMP001) 完成账户注册并审核通过",
        "time": "2分钟前"
      }
    ],
    "total": 100
  }
}
```

---

## 🎨 页面预览效果

### Desktop (PC端)

```
┌─────────────────────────────────────────────────┐
│  欢迎使用消费管理系统                              │
│  专业的企业消费管理解决方案，让数据化管理更简单高效   │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│  ℹ️ 系统当前状态                                  │
│  系统运行正常，当前已配置 8 个基础模块...           │
└─────────────────────────────────────────────────┘

┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐
│ 💰营业额│ │ 🛒订单数│ │ 👥用户 │ │ 📄客单价│
│ ¥12,580│ │   856  │ │   425  │ │ ¥14.70 │
│ +12.5% │ │ +8.3%  │ │ +15.2% │ │ -2.1%  │
└────────┘ └────────┘ └────────┘ └────────┘

┌─────────────────────────────────────────────────┐
│  📱 快速操作                                      │
│  [账户类别] [账户管理] [区域管理] [餐别资料]       │
│  [参数设置] [设备管理] [商品资料] [补贴管理]       │
│  [报表中心]                                       │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│  📋 最近活动                            [刷新]    │
│  ✅ 新用户注册                      2分钟前       │
│  💰 批量补贴发放                   15分钟前       │
│  ⚠️ 设备离线提醒                    1小时前       │
│  ✅ 数据同步完成                    2小时前       │
└─────────────────────────────────────────────────┘
```

### Mobile (移动端)

```
┌───────────────┐
│  欢迎使用      │
│  消费管理系统   │
└───────────────┘

┌───────────────┐
│ 💰 今日营业额  │
│   ¥12,580     │
│   +12.5%      │
└───────────────┘

┌───────────────┐
│ 🛒 今日订单数  │
│     856       │
│   +8.3%       │
└───────────────┘

... (其他卡片)
```

---

## ✅ 测试检查清单

在测试时请检查以下功能：

- [ ] 页面正常加载，无白屏
- [ ] 统计数据卡片显示正确
- [ ] 统计数据有加载动画（0.5秒）
- [ ] 刷新页面数据会变化（随机数）
- [ ] 快速操作卡片可以点击
- [ ] 点击快速操作有提示（功能开发中）
- [ ] 活动列表正常展示
- [ ] 点击刷新按钮可以重新加载活动
- [ ] 移动端响应式布局正常
- [ ] 卡片 hover 有动画效果
- [ ] 无控制台错误

---

## 🐛 常见问题

### Q1: 页面显示 404

**A:** 检查是否在 `routers.js` 中引入了 `consumptionRouters`

### Q2: 页面空白无数据

**A:** 打开浏览器控制台查看是否有报错，检查组件路径是否正确

### Q3: 图标不显示

**A:** 检查 Ant Design Icons 是否正确引入

### Q4: 点击快速操作卡片报错

**A:** 快速操作中的其他页面还未创建，点击会提示"功能开发中"

---

## 📞 技术支持

如有问题，请检查：
1. `src/views/business/consumption/路由配置说明.md` - 详细路由配置
2. `src/views/business/consumption/dashboard/index.vue` - 主页面代码
3. 浏览器控制台错误信息

祝测试顺利！🎉

