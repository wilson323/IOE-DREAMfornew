# OA工作流模块前端API接口设计（Web端）

## 📋 接口概述

本文档定义了OA工作流模块的Web端前端API接口规范，提供流程定义管理、流程实例管理、任务管理、流程监控等核心功能的完整接口支持。

## 🔐 认证授权

### 认证方式
- **Token认证**: Bearer Token (Sa-Token)
- **权限级别**: 基于角色的访问控制
- **认证头**: `Authorization: Bearer {token}`

### 权限验证
- **接口级权限**: 基于角色和权限的访问控制
- **数据级权限**: 基于用户的数据隔离
- **操作级权限**: 增删改查操作的细粒度控制

## 🌐 RESTful API设计

### 基础路径
```
Base URL: https://api.ioedream.com/oa/v1/workflow
```

### 通用响应格式
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {},
  "timestamp": 1640995200000
}
```

### 分页响应格式
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "list": [],
    "total": 100,
    "pageNum": 1,
    "pageSize": 20,
    "pages": 5
  }
}
```

### 错误码定义
| 错误码 | 说明 | HTTP状态码 |
|--------|------|-----------|
| 40100 | 未登录或token失效 | 401 |
| 40300 | 权限不足 | 403 |
| 40400 | 资源不存在 | 404 |
| 40900 | 流程冲突 | 409 |
| 50000 | 服务器内部错误 | 500 |
| 90001 | 流程定义不存在 | 400 |
| 90002 | 流程实例不存在 | 400 |
| 90003 | 任务不存在 | 400 |
| 90004 | 任务状态不允许操作 | 400 |
| 90005 | 流程定义未发布 | 400 |

## 📚 API接口清单

### 1. 流程定义管理

#### 1.1 部署流程定义
```http
POST /api/v1/workflow/engine/definition/deploy
Content-Type: application/x-www-form-urlencoded
```

**请求参数:**
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| bpmnXml | String | 是 | BPMN XML定义 |
| processName | String | 是 | 流程名称 |
| processKey | String | 是 | 流程Key（唯一标识） |
| description | String | 否 | 流程描述 |
| category | String | 否 | 流程分类（LEAVE/EXPENSE/PURCHASE等） |

**响应示例:**
```json
{
  "code": 200,
  "message": "流程部署成功",
  "data": "流程部署成功"
}
```

#### 1.2 分页查询流程定义
```http
GET /api/v1/workflow/engine/definition/page?pageNum=1&pageSize=20&category=LEAVE&status=PUBLISHED&keyword=请假
```

**请求参数:**
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| pageNum | Long | 否 | 页码（默认1） |
| pageSize | Long | 否 | 每页数量（默认20） |
| category | String | 否 | 流程分类 |
| status | String | 否 | 状态（DRAFT/PUBLISHED/DISABLED） |
| keyword | String | 否 | 关键词（搜索流程名称和Key） |

**响应示例:**
```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "list": [
      {
        "id": 1,
        "processKey": "LEAVE_APPROVAL",
        "processName": "请假审批流程",
        "category": "LEAVE",
        "status": "PUBLISHED",
        "instanceCount": 150,
        "createTime": "2025-01-01T10:00:00",
        "lastDeployTime": "2025-01-15T09:00:00"
      }
    ],
    "total": 100,
    "pageNum": 1,
    "pageSize": 20,
    "pages": 5
  }
}
```

#### 1.3 获取流程定义详情
```http
GET /api/v1/workflow/engine/definition/{definitionId}
```

**响应示例:**
```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "id": 1,
    "processKey": "LEAVE_APPROVAL",
    "processName": "请假审批流程",
    "bpmnXml": "<bpmn>...</bpmn>",
    "description": "员工请假审批流程",
    "category": "LEAVE",
    "status": "PUBLISHED",
    "formDefinition": "{...}",
    "variableDefinition": "{...}",
    "instanceCount": 150,
    "createTime": "2025-01-01T10:00:00"
  }
}
```

#### 1.4 激活流程定义
```http
PUT /api/v1/workflow/engine/definition/{definitionId}/activate
```

#### 1.5 禁用流程定义
```http
PUT /api/v1/workflow/engine/definition/{definitionId}/disable
```

#### 1.6 删除流程定义
```http
POST /api/v1/workflow/engine/definition/{definitionId}/delete?cascade=false
```

**请求参数:**
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| cascade | Boolean | 否 | 是否级联删除（默认false） |

### 2. 流程实例管理

#### 2.1 启动流程实例
```http
POST /api/v1/workflow/engine/instance/start
Content-Type: application/json
```

**请求参数:**
```json
{
  "definitionId": 1,
  "businessKey": "LEAVE_20250130_001",
  "instanceName": "张三请假申请",
  "variables": {
    "applicantId": 1001,
    "applicantName": "张三",
    "leaveType": "年假",
    "startDate": "2025-02-01",
    "endDate": "2025-02-05",
    "days": 5,
    "reason": "回家过年"
  },
  "formData": {
    "contactPhone": "13800138000",
    "emergencyContact": "李四",
    "emergencyPhone": "13900139000"
  }
}
```

**响应示例:**
```json
{
  "code": 200,
  "message": "流程启动成功",
  "data": 1001
}
```

#### 2.2 分页查询流程实例
```http
GET /api/v1/workflow/engine/instance/page?pageNum=1&pageSize=20&definitionId=1&status=1&startUserId=1001&startDate=2025-01-01&endDate=2025-01-31
```

**请求参数:**
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| pageNum | Long | 否 | 页码（默认1） |
| pageSize | Long | 否 | 每页数量（默认20） |
| definitionId | Long | 否 | 流程定义ID |
| status | String | 否 | 状态（1-进行中，2-已完成，3-已终止，4-已挂起） |
| startUserId | Long | 否 | 发起人ID |
| startDate | String | 否 | 开始日期（格式：yyyy-MM-dd） |
| endDate | String | 否 | 结束日期（格式：yyyy-MM-dd） |

**响应示例:**
```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "list": [
      {
        "instanceId": 1001,
        "processDefinitionId": 1,
        "processName": "请假审批流程",
        "processKey": "LEAVE_APPROVAL",
        "title": "张三请假申请",
        "initiatorId": 1001,
        "initiatorName": "张三",
        "currentNodeId": "dept_approval",
        "currentNodeName": "部门审批节点",
        "status": 1,
        "statusDesc": "进行中",
        "priority": 2,
        "startTime": "2025-01-30T10:00:00",
        "estimatedEndTime": "2025-02-01T18:00:00"
      }
    ],
    "total": 50,
    "pageNum": 1,
    "pageSize": 20,
    "pages": 3
  }
}
```

#### 2.3 获取流程实例详情
```http
GET /api/v1/workflow/engine/instance/{instanceId}
```

#### 2.4 挂起流程实例
```http
PUT /api/v1/workflow/engine/instance/{instanceId}/suspend?reason=需要补充材料
```

#### 2.5 激活流程实例
```http
PUT /api/v1/workflow/engine/instance/{instanceId}/activate
```

#### 2.6 终止流程实例
```http
PUT /api/v1/workflow/engine/instance/{instanceId}/terminate?reason=申请人撤回
```

#### 2.7 撤销流程实例
```http
PUT /api/v1/workflow/engine/instance/{instanceId}/revoke?reason=申请错误
```

### 3. 任务管理

#### 3.1 分页查询我的待办任务
```http
GET /api/v1/workflow/engine/task/my/pending?pageNum=1&pageSize=20&category=LEAVE&priority=3&dueStatus=OVERDUE
```

**请求参数:**
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| pageNum | Long | 否 | 页码（默认1） |
| pageSize | Long | 否 | 每页数量（默认20） |
| category | String | 否 | 流程分类 |
| priority | Integer | 否 | 优先级（1-低，2-普通，3-高，4-紧急） |
| dueStatus | String | 否 | 到期状态（OVERDUE-已过期，DUE_SOON-即将到期，NORMAL-正常） |

**响应示例:**
```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "list": [
      {
        "taskId": 2001,
        "instanceId": 1001,
        "taskName": "部门经理审批",
        "taskKey": "dept_approval",
        "nodeId": "dept_approval",
        "nodeName": "部门审批节点",
        "nodeType": 2,
        "nodeTypeDesc": "审批任务",
        "assigneeId": 1002,
        "assigneeName": "李经理",
        "status": 1,
        "statusDesc": "待处理",
        "priority": 3,
        "priorityDesc": "高",
        "dueTime": "2025-01-31T18:00:00",
        "isOverdue": false,
        "processName": "请假审批流程",
        "title": "张三请假申请",
        "initiatorName": "张三",
        "taskCreateTime": "2025-01-30T10:00:00"
      }
    ],
    "total": 15,
    "pageNum": 1,
    "pageSize": 20,
    "pages": 1
  }
}
```

#### 3.2 分页查询我的已办任务
```http
GET /api/v1/workflow/engine/task/my/completed?pageNum=1&pageSize=20&category=LEAVE&outcome=1&startDate=2025-01-01&endDate=2025-01-31
```

**请求参数:**
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| pageNum | Long | 否 | 页码（默认1） |
| pageSize | Long | 否 | 每页数量（默认20） |
| category | String | 否 | 流程分类 |
| outcome | String | 否 | 处理结果（1-同意，2-驳回，3-转办，4-委派） |
| startDate | String | 否 | 开始日期（格式：yyyy-MM-dd） |
| endDate | String | 否 | 结束日期（格式：yyyy-MM-dd） |

#### 3.3 分页查询我发起的流程
```http
GET /api/v1/workflow/engine/instance/my?pageNum=1&pageSize=20&category=LEAVE&status=1
```

#### 3.4 获取任务详情
```http
GET /api/v1/workflow/engine/task/{taskId}
```

**响应示例:**
```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "taskId": 2001,
    "instanceId": 1001,
    "taskName": "部门经理审批",
    "nodeName": "部门审批节点",
    "assigneeName": "李经理",
    "status": 1,
    "statusDesc": "待处理",
    "priority": 3,
    "priorityDesc": "高",
    "dueTime": "2025-01-31T18:00:00",
    "taskData": "{...}",
    "formData": "{...}",
    "processName": "请假审批流程",
    "title": "张三请假申请",
    "initiatorName": "张三"
  }
}
```

#### 3.5 受理任务
```http
PUT /api/v1/workflow/engine/task/{taskId}/claim
```

**说明**: 自动从请求中获取当前用户ID

#### 3.6 取消受理任务
```http
PUT /api/v1/workflow/engine/task/{taskId}/unclaim
```

#### 3.7 委派任务
```http
PUT /api/v1/workflow/engine/task/{taskId}/delegate?targetUserId=1002
```

**请求参数:**
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| targetUserId | Long | 是 | 目标用户ID |

#### 3.8 转交任务
```http
PUT /api/v1/workflow/engine/task/{taskId}/transfer?targetUserId=1002
```

#### 3.9 完成任务
```http
POST /api/v1/workflow/engine/task/{taskId}/complete
Content-Type: application/json
```

**请求参数:**
```json
{
  "outcome": "同意",
  "comment": "同意请假申请，请按时返岗",
  "variables": {
    "approved": true,
    "nextApprover": 1003,
    "approvalLevel": 2
  },
  "formData": {
    "approvalTime": "2025-01-30T14:30:00",
    "approvalSignature": "李经理"
  }
}
```

**请求参数说明:**
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| outcome | String | 否 | 处理结果（默认"同意"） |
| comment | String | 否 | 处理意见 |
| variables | Map | 否 | 流程变量 |
| formData | Map | 否 | 表单数据 |

#### 3.10 驳回任务
```http
POST /api/v1/workflow/engine/task/{taskId}/reject
Content-Type: application/json
```

**请求参数:**
```json
{
  "comment": "请假天数过长，不予批准",
  "variables": {
    "rejectReason": "天数超限",
    "suggestDays": 3
  }
}
```

**请求参数说明:**
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| comment | String | 是 | 驳回意见（必填） |
| variables | Map | 否 | 流程变量 |

### 4. 流程监控

#### 4.1 获取流程实例图
```http
GET /api/v1/workflow/engine/instance/{instanceId}/diagram
```

**响应示例:**
```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "instanceId": 1001,
    "processKey": "LEAVE_APPROVAL",
    "processName": "请假审批流程",
    "currentNodeId": "dept_approval",
    "currentNodeName": "部门审批节点",
    "status": 1,
    "statusDesc": "进行中",
    "currentTaskId": 2001,
    "currentTaskName": "部门经理审批",
    "currentAssignee": "李经理",
    "bpmnXml": "<bpmn>...</bpmn>"
  }
}
```

#### 4.2 获取流程历史记录
```http
GET /api/v1/workflow/engine/instance/{instanceId}/history
```

**响应示例:**
```json
{
  "code": 200,
  "message": "查询成功",
  "data": [
    {
      "id": 2001,
      "name": "部门经理审批",
      "assignee": "李经理",
      "status": 3,
      "comment": "同意",
      "startTime": "2025-01-30T10:00:00",
      "endTime": "2025-01-30T14:30:00",
      "duration": 16200000
    },
    {
      "id": 2000,
      "name": "提交申请",
      "assignee": "张三",
      "status": 3,
      "comment": "提交请假申请",
      "startTime": "2025-01-30T09:00:00",
      "endTime": "2025-01-30T10:00:00",
      "duration": 3600000
    }
  ]
}
```

#### 4.3 获取流程统计信息
```http
GET /api/v1/workflow/engine/statistics?startDate=2025-01-01&endDate=2025-01-31
```

**响应示例:**
```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "total": 1000,
    "running": 150,
    "completed": 800,
    "terminated": 30,
    "suspended": 20,
    "avgDuration": 86400000,
    "completionRate": "80.00%"
  }
}
```

#### 4.4 获取用户工作量统计
```http
GET /api/v1/workflow/engine/statistics/user/{userId}?startDate=2025-01-01&endDate=2025-01-31
```

**响应示例:**
```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "totalTasks": 50,
    "pendingTasks": 5,
    "processingTasks": 3,
    "completedTasks": 42,
    "avgDuration": 3600000,
    "overdueCount": 2
  }
}
```

## 🎨 Web端UI设计规范

### 设计原则
- **现代化**: 采用Ant Design设计语言，符合企业级应用标准
- **响应式**: 支持PC、平板多端适配
- **易用性**: 操作简单直观，减少用户学习成本
- **美观性**: 界面美观大方，色彩搭配合理

### 核心页面设计

#### 1. 待办任务列表页

**页面布局:**
```
┌─────────────────────────────────────────────────┐
│  待办任务                    [筛选] [搜索] [刷新] │
├─────────────────────────────────────────────────┤
│  ┌─────────────┐ ┌─────────────┐ ┌──────────┐ │
│  │ 全部(15)    │ │ 紧急(3)     │ │ 已过期(2) │ │
│  └─────────────┘ └─────────────┘ └──────────┘ │
├─────────────────────────────────────────────────┤
│  ┌───────────────────────────────────────────┐ │
│  │ [紧急] 部门经理审批                      │ │
│  │ 请假审批流程 - 张三请假申请              │ │
│  │ 发起人: 张三 | 截止: 2025-01-31 18:00    │ │
│  │ [受理] [转办] [委派] [查看详情]          │ │
│  └───────────────────────────────────────────┘ │
│  ┌───────────────────────────────────────────┐ │
│  │ [高] 财务审批                            │ │
│  │ 报销审批流程 - 李四差旅费报销            │ │
│  │ 发起人: 李四 | 截止: 2025-02-01 12:00    │ │
│  │ [受理] [转办] [委派] [查看详情]          │ │
│  └───────────────────────────────────────────┘ │
└─────────────────────────────────────────────────┘
```

**设计要点**:
- 卡片式布局，每个任务一张卡片
- 优先级颜色标识（紧急-红色，高-橙色，普通-蓝色，低-灰色）
- 到期时间倒计时显示
- 快速操作按钮（受理、转办、委派、查看详情）
- 筛选和搜索功能
- 支持批量操作

**UI组件**:
- 使用Ant Design的Card、Tag、Button、Badge组件
- 使用Timeline显示任务流程
- 使用Table支持列表视图切换

#### 2. 任务详情页

**页面布局:**
```
┌─────────────────────────────────────────────────┐
│  任务详情 - 部门经理审批                        │
├──────────────┬──────────────────────────────────┤
│              │  流程进度                         │
│  任务信息    │  ┌──────────────────────────┐  │
│  ┌────────┐  │  │ ○ 提交申请 (已完成)      │  │
│  │ 状态   │  │  │ ├─ 张三 2025-01-30 09:00 │  │
│  │ 待处理 │  │  │                          │  │
│  └────────┘  │  │ ● 部门审批 (当前)        │  │
│  ┌────────┐  │  │ ├─ 李经理 待处理         │  │
│  │ 优先级 │  │  │                          │  │
│  │ 高     │  │  │ ○ 人事审批 (待处理)      │  │
│  └────────┘  │  │                          │  │
│  ┌────────┐  │  │ ○ 完成 (待处理)         │  │
│  │ 截止时间│  │  └──────────────────────────┘  │
│  │ 2025-01-│  │                                  │
│  │ 31 18:00│  │  审批表单                        │
│  └────────┘  │  ┌──────────────────────────┐  │
│              │  │ 处理意见: [输入框]        │  │
│              │  │ [同意] [驳回] [转办]     │  │
│              │  └──────────────────────────┘  │
│              │                                  │
│              │  历史记录                         │
│              │  ┌──────────────────────────┐  │
│              │  │ 张三: 提交申请 (已完成)   │  │
│              │  │ 时间: 2025-01-30 09:00   │  │
│              │  └──────────────────────────┘  │
└──────────────┴──────────────────────────────────┘
```

**设计要点**:
- 左侧：任务基本信息、流程进度图
- 右侧：审批表单、历史记录
- 底部：操作按钮（同意、驳回、转办、委派）
- 实时更新任务状态
- 支持流程图可视化

**UI组件**:
- 使用Ant Design的Descriptions、Steps、Form、Timeline组件
- 使用流程图可视化库（bpmn-js或自定义流程图组件）

#### 3. 流程实例列表页

**页面布局:**
```
┌─────────────────────────────────────────────────┐
│  流程实例                    [新建] [筛选] [导出] │
├─────────────────────────────────────────────────┤
│  [全部] [进行中] [已完成] [已终止] [已挂起]     │
├─────────────────────────────────────────────────┤
│  流程名称    │ 发起人 │ 状态 │ 当前节点 │ 操作  │
├─────────────┼────────┼──────┼──────────┼───────┤
│ 张三请假申请 │ 张三   │进行中│部门审批  │[详情] │
│ 李四报销申请 │ 李四   │已完成│完成      │[详情] │
│ 王五采购申请 │ 王五   │已终止│终止      │[详情] │
└─────────────────────────────────────────────────┘
```

**设计要点**:
- 表格展示，支持排序和筛选
- 状态标签（进行中-蓝色，已完成-绿色，已终止-红色，已挂起-灰色）
- 快速操作（查看详情、挂起、终止、撤销）
- 批量操作支持
- 支持导出Excel

**UI组件**:
- 使用Ant Design的Table、Tag、Dropdown、Button组件

#### 4. 流程定义管理页

**页面布局:**
```
┌─────────────────────────────────────────────────┐
│  流程定义管理              [新建] [导入] [发布]  │
├─────────────────────────────────────────────────┤
│  ┌───────────────────────────────────────────┐ │
│  │ 流程设计器 (BPMN编辑器)                    │ │
│  │                                            │ │
│  │  [拖拽节点] [连线] [属性设置]              │ │
│  │                                            │ │
│  └───────────────────────────────────────────┘ │
│  ┌───────────────────────────────────────────┐ │
│  │ 流程列表                                  │ │
│  │ [请假审批] [报销审批] [采购审批] ...      │ │
│  └───────────────────────────────────────────┘ │
└─────────────────────────────────────────────────┘
```

**设计要点**:
- 流程列表展示
- 流程设计器（BPMN编辑器）
- 流程预览和测试
- 版本管理
- 流程发布和禁用

**UI组件**:
- 使用bpmn-js流程设计器
- 使用Ant Design的Modal、Tabs、Tree组件

## 🎯 用户体验优化

### 1. 性能优化
- **接口响应时间**: < 500ms
- **列表加载**: 支持虚拟滚动，优化大数据量展示
- **图片懒加载**: 流程图片和附件支持懒加载
- **缓存策略**: 合理使用缓存，减少重复请求
- **分页优化**: 支持前端分页和后端分页切换

### 2. 交互优化
- **实时通知**: WebSocket推送任务通知
- **操作反馈**: 所有操作都有明确的成功/失败反馈
- **加载状态**: 所有异步操作显示加载状态
- **错误提示**: 友好的错误提示信息
- **确认对话框**: 重要操作（删除、终止等）需要确认

### 3. 视觉优化
- **色彩系统**: 统一的色彩规范
  - 主色：蓝色 (#1890ff)
  - 成功：绿色 (#52c41a)
  - 警告：橙色 (#faad14)
  - 错误：红色 (#f5222d)
- **图标系统**: 统一的图标库（Ant Design Icons）
- **动画效果**: 适度的动画效果，提升体验
- **响应式布局**: 完美适配各种屏幕尺寸

### 4. 功能增强
- **批量操作**: 支持批量审批、批量转办
- **快捷操作**: 支持快捷键操作
  - `Ctrl + Enter`: 快速同意
  - `Ctrl + R`: 快速驳回
  - `Ctrl + T`: 快速转办
- **搜索功能**: 强大的搜索和筛选功能
- **导出功能**: 支持导出审批记录（Excel/PDF）
- **打印功能**: 支持打印审批单

## 🔔 WebSocket实时通信

### 连接地址
```
ws://api.ioedream.com/oa/v1/workflow/ws
```

### 消息类型

#### 1. 新任务通知
```json
{
  "type": "NEW_TASK",
  "data": {
    "taskId": 2001,
    "taskName": "部门经理审批",
    "processName": "请假审批流程",
    "title": "张三请假申请",
    "priority": 3,
    "dueTime": "2025-01-31T18:00:00",
    "timestamp": 1640995200000
  }
}
```

#### 2. 任务状态变更
```json
{
  "type": "TASK_STATUS_CHANGED",
  "data": {
    "taskId": 2001,
    "status": 3,
    "statusDesc": "已完成",
    "updateTime": "2025-01-30T14:30:00",
    "timestamp": 1640995200000
  }
}
```

#### 3. 流程实例状态变更
```json
{
  "type": "INSTANCE_STATUS_CHANGED",
  "data": {
    "instanceId": 1001,
    "status": 2,
    "statusDesc": "已完成",
    "updateTime": "2025-01-30T15:00:00",
    "timestamp": 1640995200000
  }
}
```

## 📊 数据字典

### 任务状态
| 值 | 说明 | 颜色 | 图标 |
|---|------|------|------|
| 1 | 待处理 | 蓝色 (#1890ff) | clock-circle |
| 2 | 处理中 | 橙色 (#faad14) | loading |
| 3 | 已完成 | 绿色 (#52c41a) | check-circle |
| 4 | 已转办 | 紫色 (#722ed1) | swap |
| 5 | 已委派 | 青色 (#13c2c2) | team |
| 6 | 已驳回 | 红色 (#f5222d) | close-circle |

### 流程实例状态
| 值 | 说明 | 颜色 | 图标 |
|---|------|------|------|
| 1 | 进行中 | 蓝色 (#1890ff) | play-circle |
| 2 | 已完成 | 绿色 (#52c41a) | check-circle |
| 3 | 已终止 | 红色 (#f5222d) | stop |
| 4 | 已挂起 | 灰色 (#8c8c8c) | pause-circle |

### 优先级
| 值 | 说明 | 颜色 | 图标 |
|---|------|------|------|
| 1 | 低 | 灰色 (#8c8c8c) | arrow-down |
| 2 | 普通 | 蓝色 (#1890ff) | minus |
| 3 | 高 | 橙色 (#faad14) | arrow-up |
| 4 | 紧急 | 红色 (#f5222d) | exclamation-circle |

### 流程定义状态
| 值 | 说明 | 颜色 |
|---|------|------|
| DRAFT | 草稿 | 灰色 |
| PUBLISHED | 已发布 | 绿色 |
| DISABLED | 已禁用 | 红色 |

## ✅ 接口测试

### 测试工具
- **Postman**: API接口测试
- **Apifox**: API文档和测试
- **Knife4j**: Swagger UI在线测试

### 测试用例
1. **流程定义部署测试**
   - 正常部署流程
   - 流程Key重复测试
   - BPMN XML格式验证

2. **流程实例启动测试**
   - 正常启动流程
   - 流程定义未发布测试
   - 参数验证测试

3. **任务受理和完成测试**
   - 正常受理任务
   - 任务状态验证
   - 完成任务流程

4. **任务转办和委派测试**
   - 正常转办任务
   - 正常委派任务
   - 目标用户验证

5. **流程监控功能测试**
   - 流程图获取
   - 历史记录查询
   - 统计信息查询

---

**文档版本**: v1.0.0  
**最后更新**: 2025-01-30  
**维护人**: 前端团队
