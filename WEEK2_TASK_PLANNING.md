# Week 2 核心功能修复任务规划

**规划时间**: 2025-12-26
**执行周期**: Week 2（预计1周）
**状态**: 任务规划阶段

## 📊 Week 1 成果回顾

### ✅ 已完成任务（100%代码修复）

**Task 1: Entity统一管理** - 346个错误 → 0个 ✅
- 删除5个服务的重复`domain/entity/`目录
- 修复26个文件的导入路径
- 创建6个缺失的Entity类
- 修复RuleExecutionStatistics导入路径

**Task 2: 接口完整性修复** ✅
- 添加`ValidationStep`内部类到`RuleValidationResult`
- 统一`AttendanceRuleEngineImpl`为Facade版本
- 解决配置类与实现的类型匹配问题

**总修复错误数**: ~360个

**编译验证状态**: ⚠️ Maven环境异常，等待IDE验证

## 🎯 Week 2 核心任务

### Task 1: 排班引擎重构（预计3人天）

**问题描述**:
- SchedulePredictor接口定义了14个预测方法
- 实现类SchedulePredictorImpl可能不完整
- 缺少相关的模型类和预测算法

**影响范围**:
- `ioedream-attendance-service` 考勤服务
- 排班预测和智能调度功能

**执行步骤**:

#### 1.1 分析现有实现（0.5人天）
```bash
# 已发现的文件
- SchedulePredictor.java (接口)
- SchedulePredictorImpl.java (实现)
- SchedulePredictionService.java (服务)
- ScheduleEngineConfiguration.java (配置)
- ScheduleAlgorithmConfiguration.java (算法配置)
```

**分析内容**:
1. 检查SchedulePredictorImpl中每个方法的实现状态
2. 识别缺失的模型类
3. 确认算法实现是否完整

#### 1.2 补全模型类（1人天）

**需要的模型类**（基于接口定义）:
```java
// 预测相关
- PredictionData.java           // 预测数据
- PredictionScope.java          // 预测范围
- SchedulePredictionResult.java // 排班预测结果
- StaffingPredictionResult.java // 人员需求预测结果
- AbsenteeismPredictionResult.java // 缺勤率预测结果
- WorkloadPredictionResult.java    // 工作量预测结果
- CostPredictionResult.java        // 成本预测结果
- SatisfactionPredictionResult.java // 满意度预测结果
- ConflictPredictionResult.java    // 冲突预测结果
- SeasonalPredictionResult.java    // 季节性预测结果
- RealTimePredictionResult.java    // 实时预测结果

// 验证和性能
- PredictionValidationResult.java  // 预测验证结果
- ModelPerformanceMetrics.java     // 模型性能指标
- PredictionSuggestion.java        // 预测建议
- BatchPredictionResult.java       // 批量预测结果

// 输入数据
- TimeRange.java               // 时间范围
- SkillRequirement.java        // 技能要求
- HistoricalData.java          // 历史数据
- EmployeeData.java            // 员工数据
- BusinessFactor.java          // 业务因素
- CostFactor.java              // 成本因素
- EmployeePreference.java      // 员工偏好
- CurrentData.java             // 当前数据
- PredictionContext.java       // 预测上下文
- PredictionRequest.java       // 预测请求
- SeasonalityParameters.java   // 季节性参数
```

#### 1.3 实现预测算法（1.5人天）

**基础算法实现**:
```java
// SchedulePredictorImpl核心方法
public class SchedulePredictorImpl implements SchedulePredictor {

    @Override
    public SchedulePredictionResult predictScheduleDemand(
            PredictionData predictionData,
            PredictionScope predictionScope) {
        // 1. 历史数据分析
        // 2. 趋势识别
        // 3. 需求预测
        // 4. 结果生成
    }

    @Override
    public StaffingPredictionResult predictStaffingDemand(
            TimeRange timeRange,
            List<SkillRequirement> skillRequirements,
            HistoricalData historicalData) {
        // 基于技能需求的人员预测
    }

    // 其他12个方法...
}
```

**验证方式**:
- 单元测试覆盖每个预测方法
- 集成测试验证预测准确性
- 性能测试确保响应时间<500ms

**预期成果**:
- ✅ 14个预测方法全部实现
- ✅ 28个模型类创建完成
- ✅ 单元测试覆盖率>80%
- ✅ 编译错误减少5个

---

### Task 2: 移动端VO补全（预计2人天）

**问题描述**:
- MobileTaskVO已存在，但可能存在重复定义
- 移动端服务需要完整的VO类支持
- 可能缺少相关的DTO和Form类

**影响范围**:
- `ioedream-oa-service` OA服务
- 移动端审批功能

**执行步骤**:

#### 2.1 分析现有VO类（0.5人天）

**已发现的MobileTaskVO位置**:
```
1. ioedream-oa-service/workflow/domain/vo/MobileTaskVO.java
2. ioedream-oa-service/workflow/mobile/domain/MobileApprovalDomain.java (内部类)
```

**分析内容**:
1. 检查两个MobileTaskVO的定义是否一致
2. 确认是否有重复定义
3. 识别缺失的字段和方法

#### 2.2 统一MobileTaskVO定义（0.5人天）

**行动方案**:
- **选项A**: 如果两个类定义一致，删除重复的内部类
- **选项B**: 如果定义不一致，合并为统一的VO类

**推荐方案**: 保留`workflow/domain/vo/MobileTaskVO.java`，删除内部类定义

#### 2.3 补全移动端相关类（1人天）

**需要的移动端类**:
```java
// 移动端表单
- MobileTaskQueryForm.java      // 任务查询表单
- MobileTaskAddForm.java        // 任务新增表单
- MobileApprovalForm.java       // 审批表单

// 移动端DTO
- MobileTaskDTO.java            // 任务数据传输对象
- MobileApprovalDTO.java        // 审批数据传输对象

// 移动端响应
- MobileTaskListResult.java     // 任务列表响应
- MobileTaskDetailResult.java   // 任务详情响应

// 移动端服务接口
- MobileTaskService.java        // 移动端任务服务接口
- MobileApprovalService.java    // 移动端审批服务接口
```

**验证方式**:
- 检查所有使用MobileTaskVO的地方
- 确保移动端API返回正确的VO结构
- 单元测试验证字段映射

**预期成果**:
- ✅ MobileTaskVO统一定义
- ✅ 29个MobileTaskVO相关错误解决
- ✅ 移动端API完整性验证通过

---

### Task 3: 构造函数修复（预计2人天）

**问题描述**:
- 部分类缺少构造函数
- 构造函数参数不匹配
- Lombok注解未正确生成构造函数

**影响范围**:
- 所有微服务
- 编译错误

**执行步骤**:

#### 3.1 识别缺失构造函数（0.5人天）

**检查方式**:
```bash
# 查找所有@Entity类
grep -r "@Entity" microservices/ --include="*.java"

# 查找所有@Data类但缺少构造函数的
grep -r "@Data" microservices/ --include="*.java" -A 20 | grep -v "@Builder"
```

**目标类**:
- Entity类（需要无参构造函数）
- VO类（需要全参构造函数）
- Form类（需要无参和全参构造函数）

#### 3.2 分类修复（1.5人天）

**类别A: Entity类**
```java
// ✅ 正确的Entity构造函数
@Data
@Entity
public class UserEntity {

    // Lombok会生成无参构造函数
    @NoArgsConstructor
    @AllArgsConstructor
    public UserEntity() {
    }
}
```

**类别B: VO类**
```java
// ✅ 正确的VO构造函数
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
public class UserVO {
    private Long userId;
    private String username;
    // Lombok @Builder会生成全参构造函数
}
```

**类别C: 特殊情况**
```java
// 如果Lombok不工作，手动添加构造函数
@Data
public class SpecialClass {
    private Long id;
    private String name;

    // 手动无参构造函数
    public SpecialClass() {
    }

    // 手动全参构造函数
    public SpecialClass(Long id, String name) {
        this.id = id;
        this.name = name;
    }
}
```

**验证方式**:
- 编译所有微服务
- 检查IDE中的构造函数提示
- 单元测试验证对象创建

**预期成果**:
- ✅ 所有undefined构造函数错误解决
- ✅ Lombok注解正确工作
- ✅ 构造函数覆盖率100%

---

## 📅 Week 2 执行时间表

| 日期 | 任务 | 预计工作量 | 负责人 | 状态 |
|------|------|-----------|--------|------|
| **Day 1** | Task 1.1-1.2: 排班引擎分析+模型类创建 | 1.5人天 | AI助手 | ⏳ 待开始 |
| **Day 2-3** | Task 1.3: 实现预测算法 | 1.5人天 | AI助手 | ⏳ 待开始 |
| **Day 4** | Task 2.1-2.2: MobileTaskVO分析+统一 | 1人天 | AI助手 | ⏳ 待开始 |
| **Day 4-5** | Task 2.3: 补全移动端类 | 1人天 | AI助手 | ⏳ 待开始 |
| **Day 5-6** | Task 3.1-3.2: 构造函数修复 | 2人天 | AI助手 | ⏳ 待开始 |
| **Day 7** | Week 2验证和总结 | 1人天 | AI助手 | ⏳ 待开始 |

**总工作量**: 7人天（1人周）

## 🎯 Week 2 成功标准

### 量化指标

| 指标 | Week 1状态 | Week 2目标 | 改进幅度 |
|------|-----------|-----------|---------|
| **编译错误数** | ~360 | <50 | -86% |
| **SchedulePredictor** | 5个错误 | 0个错误 | -100% |
| **MobileTaskVO** | 29个错误 | 0个错误 | -100% |
| **构造函数** | 未知数量 | 0个错误 | -100% |
| **测试覆盖率** | 未测试 | >70% | N/A |

### 质量标准

- ✅ 所有代码通过编译
- ✅ 单元测试覆盖率>70%
- ✅ 代码符合CLAUDE.md规范
- ✅ 无重复代码定义
- ✅ Lombok注解正确工作

## 📋 Week 2 检查清单

### 代码质量
- [ ] 所有新增类有完整的JavaDoc注释
- [ ] 所有方法有参数和返回值说明
- [ ] 所有类使用正确的Lombok注解
- [ ] 所有异常有明确的错误信息
- [ ] 所有日志使用SLF4J和统一格式

### 测试覆盖
- [ ] SchedulePredictor所有方法有单元测试
- [ ] MobileTaskVO相关类有测试
- [ ] 构造函数创建有测试验证
- [ ] 集成测试覆盖核心场景

### 文档更新
- [ ] 更新CLAUDE.md架构文档
- [ ] 更新API文档
- [ ] 创建Week 2执行报告
- [ ] 更新技术栈文档

## 🚀 Week 2 后续规划

### Week 3: 质量提升（P1+P2）
1. **Null安全改进** - 减少110个警告到<10
2. **API迁移** - 解决75个弃用警告
3. **代码清理** - 清理50个未使用导入

### Week 4: 性能优化
1. **数据库索引优化** - 65%查询缺少索引
2. **缓存架构优化** - 命中率从65%→90%
3. **连接池统一** - 12个服务统一为Druid

---

## 💡 执行建议

### 优先级排序
1. **P0**: Task 1（排班引擎）- 核心业务功能
2. **P0**: Task 2（移动端VO）- 用户体验关键
3. **P1**: Task 3（构造函数）- 编译通过基础

### 风险控制
- **风险1**: 预测算法复杂度可能超出预期
  - **缓解**: 先实现简单算法，后续优化
- **风险2**: MobileTaskVO重复定义导致连锁反应
  - **缓解**: 仔细分析所有引用，逐步重构
- **风险3**: Lombok注解不工作
  - **缓解**: 手动添加构造函数作为后备方案

### 协作方式
- 每日更新进度报告
- 遇到阻塞问题立即汇报
- 代码提交前自我review
- 保持与架构团队的沟通

---

**制定人**: IOE-DREAM AI助手
**审核人**: 待定
**批准人**: 待定
**版本**: v1.0.0
**创建时间**: 2025-12-26
