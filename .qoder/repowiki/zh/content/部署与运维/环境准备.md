# 环境准备

<cite>
**本文档引用的文件**   
- [环境搭建.md](file://documentation/02-开发指南/快速开始/环境搭建.md)
- [ENVIRONMENT_VARIABLES.md](file://documentation/deployment/ENVIRONMENT_VARIABLES.md)
- [docker-compose-services.yml](file://docker-compose-services.yml)
- [docker-compose-monitoring.yml](file://deployment/monitoring/docker-compose-monitoring.yml)
- [openspec/project.md](file://openspec/project.md)
- [smart-admin-web-javascript/.env.development](file://smart-admin-web-javascript/.env.development)
- [microservices/microservices-common/src/main/java/net/lab1024/sa/common/monitor/manager/SystemMonitorManager.java](file://microservices/microservices-common/src/main/java/net/lab1024/sa/common/monitor/manager/SystemMonitorManager.java)
- [database-scripts/common-service/16-t_system_config.sql](file://database-scripts/common-service/16-t_system_config.sql)
- [database-scripts/common-service/13-t_system_monitor.sql](file://database-scripts/common-service/13-t_system_monitor.sql)
- [scripts/p0-security/scan-passwords-simple.ps1](file://scripts/p0-security/scan-passwords-simple.ps1)
- [templates/secure-application.yml](file://templates/secure-application.yml)
</cite>

## 目录
1. [硬件与操作系统要求](#硬件与操作系统要求)
2. [网络环境要求](#网络环境要求)
3. [必需依赖软件](#必需依赖软件)
4. [Docker部署方式](#docker部署方式)
5. [原生部署方式](#原生部署方式)
6. [环境变量配置](#环境变量配置)
7. [系统参数设置](#系统参数设置)
8. [环境验证检查清单](#环境验证检查清单)

## 硬件与操作系统要求

### 硬件配置要求
根据项目需求和性能要求，建议的硬件配置如下：

- **内存**: 16GB+ RAM (最低8GB)
- **硬盘**: 100GB+ 可用空间 (推荐SSD)
- **CPU**: 4核心+ (推荐8核心)

### 操作系统要求
支持以下操作系统版本：

- **Windows**: Windows 10/11
- **macOS**: 10.15+
- **Linux**: Ubuntu 18.04+ 或其他主流Linux发行版

**Section sources**
- [环境搭建.md](file://documentation/02-开发指南/快速开始/环境搭建.md#L21-L24)
- [openspec/project.md](file://openspec/project.md#L80-L85)

## 网络环境要求

### 网络带宽要求
根据系统性能指标，网络环境需满足以下要求：

- **内部网络带宽**: ≥ 1Gbps
- **外部网络带宽**: ≥ 100Mbps
- **设备通信带宽**: ≥ 10Mbps
- **视频传输带宽**: ≥ 4Mbps/路

### 网络延迟要求
- **局域网延迟**: ≤ 1毫秒
- **广域网延迟**: ≤ 50毫秒

**Section sources**
- [11-关键控制点和性能指标.md](file://documentation/03-业务模块/门禁/11-关键控制点和性能指标.md#L350-L360)

## 必需依赖软件

### 核心依赖软件
| 软件 | 版本要求 | 说明 |
|------|---------|------|
| **Java** | OpenJDK 17+ | 核心开发语言 |
| **Maven** | 3.8+ | 项目构建工具 |
| **MySQL** | 8.0+ | 主数据库 |
| **Redis** | 6.0+ | 缓存数据库 |
| **Node.js** | 18+ | 前端开发(可选) |
| **Git** | 2.30+ | 版本控制 |
| **Nacos** | v2.3.0 | 服务注册与配置中心 |

### 安装步骤
#### Java 17 安装
```bash
# Windows - 使用Chocolatey安装
choco install openjdk17

# macOS - 使用Homebrew安装
brew install openjdk@17

# Linux (Ubuntu) - 使用apt安装
sudo apt update
sudo apt install openjdk-17-jdk
```

#### Maven 3.8+ 安装
```bash
# Windows - 使用Chocolatey安装
choco install maven

# macOS - 使用Homebrew安装
brew install maven

# Linux - 使用apt安装
sudo apt install maven
```

#### MySQL 8.0 安装
```bash
# Windows - 下载官方安装包
# https://dev.mysql.com/downloads/mysql/

# macOS - 使用Homebrew安装
brew install mysql@8.0

# Linux (Ubuntu) - 使用apt安装
sudo apt update
sudo apt install mysql-server
```

#### Redis 6.0+ 安装
```bash
# Windows - 使用Chocolatey安装
choco install redis

# macOS - 使用Homebrew安装
brew install redis

# Linux - 使用apt安装
sudo apt update
sudo apt install redis-server
```

#### Node.js 18+ 安装
```bash
# Windows - 使用Chocolatey安装
choco install nodejs

# macOS - 使用Homebrew安装
brew install node@18

# Linux - 使用NodeSource仓库
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs
```

#### Git 安装
```bash
# Windows - 下载Git for Windows
# https://git-scm.com/download/win

# macOS - 使用Homebrew安装
brew install git

# Linux - 使用apt安装
sudo apt install git
```

**Section sources**
- [环境搭建.md](file://documentation/02-开发指南/快速开始/环境搭建.md#L11-L20)
- [openspec/project.md](file://openspec/project.md#L80-L85)

## Docker部署方式

### Docker环境准备
确保已安装Docker和Docker Compose：
```bash
# 验证Docker安装
docker --version

# 验证Docker Compose安装
docker-compose --version
```

### 服务部署配置
使用提供的`docker-compose-services.yml`文件部署核心服务：

```yaml
version: '3.8'

services:
  # Redis服务
  redis:
    image: redis:7-alpine
    container_name: ioedream-redis
    command: redis-server --appendonly yes
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped
    networks:
      - ioedream-network

  # Nacos服务
  nacos:
    image: nacos/nacos-server:v2.3.0
    container_name: ioedream-nacos
    environment:
      - MODE=standalone
      - SPRING_DATASOURCE_PLATFORM=mysql
      - MYSQL_SERVICE_HOST=host.docker.internal
      - MYSQL_SERVICE_PORT=3306
      - MYSQL_SERVICE_DB_NAME=nacos
      - MYSQL_SERVICE_USER=root
      - MYSQL_SERVICE_PASSWORD=root
    ports:
      - "8848:8848"
    restart: unless-stopped
    networks:
      - ioedream-network

volumes:
  redis_data:

networks:
  ioedream-network:
    external: true
    name: ioe-dream_smart-admin-network
```

### 监控服务部署
使用`docker-compose-monitoring.yml`文件部署监控服务：

```yaml
version: '3.8'

services:
  # Prometheus监控
  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./monitoring/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
      - ./monitoring/prometheus/rules:/etc/prometheus/rules
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/etc/prometheus/console_libraries'
      - '--web.console.templates=/etc/prometheus/consoles'
      - '--storage.tsdb.retention.time=30d'
      - '--web.enable-lifecycle'
    networks:
      - ioedream-network
    restart: unless-stopped

  # Grafana可视化
  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin123
      - GF_INSTALL_PLUGINS=grafana-clock-panel,grafana-simple-json-datasource
    volumes:
      - grafana_data:/var/lib/grafana
      - ./monitoring/grafana/dashboards:/etc/grafana/provisioning/dashboards
      - ./monitoring/grafana/datasources:/etc/grafana/provisioning/datasources
    networks:
      - ioedream-network
    restart: unless-stopped
    depends_on:
      - prometheus

  # AlertManager告警管理
  alertmanager:
    image: prom/alertmanager:latest
    container_name: alertmanager
    ports:
      - "9093:9093"
    volumes:
      - ./monitoring/alertmanager/alertmanager.yml:/etc/alertmanager/alertmanager.yml
      - alertmanager_data:/alertmanager
    command:
      - '--config.file=/etc/alertmanager/alertmanager.yml'
      - '--storage.path=/alertmanager'
      - '--web.external-url=http://alertmanager:9093'
    networks:
      - ioedream-network
    restart: unless-stopped

  # Node Exporter系统监控
  node-exporter:
    image: prom/node-exporter:latest
    container_name: node-exporter
    ports:
      - "9100:9100"
    command:
      - '--path.procfs=/host/proc'
      - '--path.sysfs=/host/sys'
      - '--collector.filesystem.mount-points-exclude=^/(sys|proc|dev|host|etc)($$|/)'
    volumes:
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /:/rootfs:ro
    networks:
      - ioedream-network
    restart: unless-stopped

volumes:
  prometheus_data:
  alertmanager_data:
  grafana_data:

networks:
  ioedream-network:
    external: true
```

### 启动Docker服务
```bash
# 启动核心服务
docker-compose -f docker-compose-services.yml up -d

# 启动监控服务
docker-compose -f deployment/monitoring/docker-compose-monitoring.yml up -d

# 查看服务状态
docker-compose -f docker-compose-services.yml ps
docker-compose -f deployment/monitoring/docker-compose-monitoring.yml ps
```

**Section sources**
- [docker-compose-services.yml](file://docker-compose-services.yml)
- [docker-compose-monitoring.yml](file://deployment/monitoring/docker-compose-monitoring.yml)

## 原生部署方式

### 数据库初始化
1. 创建数据库：
```sql
CREATE DATABASE IF NOT EXISTS ioe_dream CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

2. 执行数据库脚本：
```bash
# 导入数据库结构
mysql -u root -p ioe_dream < database/init.sql

# 导入基础数据
mysql -u root -p ioe_dream < database/data.sql
```

### 服务部署步骤
1. **克隆项目代码**：
```bash
git clone https://github.com/your-org/ioe-dream.git
cd ioe-dream
```

2. **构建项目**：
```bash
# 构建公共模块
cd microservices/microservices-common
mvn clean install

# 构建所有微服务
cd ../..
./scripts/build-all.ps1
```

3. **启动服务**：
```bash
# 启动Nacos服务
cd microservices/ioedream-config-service
mvn spring-boot:run

# 启动网关服务
cd ../ioedream-gateway-service
mvn spring-boot:run

# 启动其他微服务
# 依次启动各微服务模块
```

**Section sources**
- [环境搭建.md](file://documentation/02-开发指南/快速开始/环境搭建.md#L344-L348)
- [database-scripts/common-service/00-database-init.sql](file://database-scripts/common-service/00-database-init.sql)

## 环境变量配置

### 必需环境变量清单
```bash
# MySQL数据库
DB_URL=jdbc:mysql://localhost:3306/ioedream_db?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
DB_USERNAME=root
DB_PASSWORD=your_secure_password_here  # 必须设置，无默认值

# Redis配置
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=  # 可选，开发环境可为空
REDIS_DATABASE=0

# Nacos配置
NACOS_SERVER_ADDR=127.0.0.1:8848
NACOS_NAMESPACE=dev
NACOS_GROUP=IOE-DREAM
NACOS_USERNAME=nacos
NACOS_PASSWORD=  # 必须设置，已移除默认值

# JWT和认证配置
JWT_SECRET=your_jwt_secret_key_at_least_256_bits
JWT_ACCESS_EXPIRATION=86400
JWT_REFRESH_EXPIRATION=604800

SA_TOKEN_TIMEOUT=86400
SA_ACTIVE_TIMEOUT=1800
SA_JWT_SECRET=your_satoken_secret_key

# 服务端口配置
GATEWAY_PORT=8080
COMMON_SERVICE_PORT=8088
DEVICE_COMM_SERVICE_PORT=8087
OA_SERVICE_PORT=8089
ACCESS_SERVICE_PORT=8090
ATTENDANCE_SERVICE_PORT=8091
VIDEO_SERVICE_PORT=8092
CONSUME_SERVICE_PORT=8094
VISITOR_SERVICE_PORT=8095

# 分布式追踪配置
ZIPKIN_BASE_URL=http://localhost:9411
TRACING_SAMPLE_RATE=0.1
```

### 配置方法
#### 方法1: 环境变量（开发环境）
```bash
# Windows PowerShell
$env:DB_PASSWORD="your_password"
$env:NACOS_PASSWORD="your_password"

# Linux/Mac
export DB_PASSWORD="your_password"
export NACOS_PASSWORD="your_password"
```

#### 方法2: Nacos配置中心（生产环境）
1. 在Nacos配置中心创建加密配置
2. 使用AES-256加密密码
3. 配置文件中使用 `ENC(加密字符串)` 格式

**重要提示**: 修改配置后需要重启相应的微服务

**Section sources**
- [ENVIRONMENT_VARIABLES.md](file://documentation/deployment/ENVIRONMENT_VARIABLES.md)
- [templates/secure-application.yml](file://templates/secure-application.yml)

## 系统参数设置

### 文件句柄数设置
#### Linux系统
```bash
# 临时设置
ulimit -n 65536

# 永久设置 - 编辑 /etc/security/limits.conf
echo "* soft nofile 65536" >> /etc/security/limits.conf
echo "* hard nofile 65536" >> /etc/security/limits.conf

# 重启系统或重新登录生效
```

#### Windows系统
Windows系统默认文件句柄数通常足够，如需调整可通过注册表修改。

### 内存限制设置
#### JVM内存设置
在启动微服务时设置JVM参数：
```bash
# 设置JVM内存
export JAVA_OPTS="-Xms512m -Xmx2048m -XX:MetaspaceSize=256m -XX:MaxMetaspaceSize=512m"

# 启动服务
mvn spring-boot:run
```

#### 系统级内存限制
根据硬件配置合理分配内存资源，建议为数据库和缓存服务预留足够内存。

### 系统监控参数
系统监控功能支持以下参数配置：

```java
// 资源使用监控参数
public class ResourceUsageVO {
    private Double cpuUsage;        // CPU使用率（百分比）
    private Integer cpuCores;       // CPU核心数
    private Double cpuLoad;         // CPU负载
    private Double totalMemory;     // 总内存（GB）
    private Double usedMemory;      // 已用内存（GB）
    private Double availableMemory; // 可用内存（GB）
    private Double memoryUsage;     // 内存使用率（百分比）
    private Double totalDisk;       // 总磁盘空间（GB）
    private Double usedDisk;        // 已用磁盘空间（GB）
    private Double availableDisk;   // 可用磁盘空间（GB）
    private Double diskUsage;       // 磁盘使用率（百分比）
    private Double networkInbound;  // 网络入站流量（MB/s）
    private Double networkOutbound; // 网络出站流量（MB/s）
}
```

**Section sources**
- [SystemMonitorManager.java](file://microservices/microservices-common/src/main/java/net/lab1024/sa/common/monitor/manager/SystemMonitorManager.java)
- [ResourceUsageVO.java](file://microservices/microservices-common/src/main/java/net/lab1024/sa/common/monitor/domain/vo/ResourceUsageVO.java)

## 环境验证检查清单

### 基础环境验证
```bash
#!/bin/bash
# env-check.sh

echo "🔍 环境检查开始..."

# 检查Java
if command -v java &> /dev/null; then
    echo "✅ Java: $(java -version 2>&1 | head -n 1)"
else
    echo "❌ Java 未安装"
fi

# 检查Maven
if command -v mvn &> /dev/null; then
    echo "✅ Maven: $(mvn -version 2>&1 | head -n 1)"
else
    echo "❌ Maven 未安装"
fi

# 检查MySQL
if command -v mysql &> /dev/null; then
    echo "✅ MySQL: $(mysql --version)"
else
    echo "❌ MySQL 未安装"
fi

# 检查Redis
if command -v redis-cli &> /dev/null; then
    echo "✅ Redis: $(redis-cli --version)"
else
    echo "❌ Redis 未安装"
fi

# 检查Node.js
if command -v node &> /dev/null; then
    echo "✅ Node.js: $(node --version)"
else
    echo "❌ Node.js 未安装 (前端开发可选)"
fi

echo "🔍 环境检查完成"
```

### 服务状态验证
1. **数据库服务**：
   ```bash
   # 测试MySQL连接
   mysql -h localhost -u root -p -e "SELECT VERSION();"
   ```

2. **Redis服务**：
   ```bash
   # 测试Redis连接
   redis-cli ping
   # 应返回 PONG
   ```

3. **Nacos服务**：
   ```bash
   # 验证Nacos健康状态
   curl -f http://localhost:8848/nacos/v1/console/health/readiness
   ```

4. **微服务健康检查**：
   ```bash
   # 检查网关服务
   curl -f http://localhost:8080/actuator/health
   
   # 检查公共服务
   curl -f http://localhost:8088/actuator/health
   ```

### 监控系统验证
1. **Prometheus验证**：
   ```bash
   # 验证Prometheus健康状态
   curl -f http://localhost:9090/-/healthy
   ```

2. **Grafana验证**：
   ```bash
   # 验证Grafana健康状态
   curl -f http://localhost:3000/api/health
   ```

3. **AlertManager验证**：
   ```bash
   # 验证AlertManager健康状态
   curl -f http://localhost:9093/-/healthy
   ```

### 完整检查清单
| 检查项 | 验证方法 | 预期结果 | 状态 |
|--------|---------|---------|------|
| Java 17 | `java -version` | 显示 openjdk version "17.x.x" | |
| Maven 3.8+ | `mvn -version` | 显示 Apache Maven 3.8+ | |
| MySQL 8.0+ | `mysql --version` | 显示 MySQL 8.0+ 版本 | |
| Redis 6.0+ | `redis-cli --version` | 显示 Redis 6.0+ 版本 | |
| Node.js 18+ | `node --version` | 显示 v18.x.x | |
| Git | `git --version` | 显示 Git 2.30+ 版本 | |
| Docker | `docker --version` | 显示 Docker 版本 | |
| Docker Compose | `docker-compose --version` | 显示 Docker Compose 版本 | |
| Nacos服务 | `curl http://localhost:8848` | 返回健康状态 | |
| Redis服务 | `redis-cli ping` | 返回 PONG | |
| MySQL服务 | `mysql -u root -p -e "status"` | 显示数据库状态 | |
| 网关服务 | `curl http://localhost:8080/actuator/health` | 返回健康状态 | |
| 公共服务 | `curl http://localhost:8088/actuator/health` | 返回健康状态 | |
| Prometheus | `curl http://localhost:9090/-/healthy` | 返回正常 | |
| Grafana | `curl http://localhost:3000/api/health` | 返回正常 | |
| AlertManager | `curl http://localhost:9093/-/healthy` | 返回正常 | |

**Section sources**
- [env-check.sh](file://scripts/env-check.sh)
- [环境搭建.md](file://documentation/02-开发指南/快速开始/环境搭建.md#L452-L497)
- [docker-compose-monitoring.yml](file://deployment/monitoring/docker-compose-monitoring.yml)