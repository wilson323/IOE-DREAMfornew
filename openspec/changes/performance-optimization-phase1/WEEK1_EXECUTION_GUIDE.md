# P1性能优化 Week 1 执行指南

**执行日期**: 2025-01-XX
**执行阶段**: Week 1 - 数据库和缓存优化
**预计工时**: 5个工作日
**负责人**: 后端团队 + DBA团队

---

## 📋 任务概览

### 已完成的工作

✅ **1. 慢查询分析脚本**
- 文件位置: `openspec/changes/performance-optimization-phase1/scripts/analyze_slow_queries.sql`
- 功能: 分析数据库慢查询，识别需要优化的查询

✅ **2. 索引创建脚本**
- 文件位置: `openspec/changes/performance-optimization-phase1/scripts/create_indexes.sql`
- 功能: 创建25个优化索引（门禁、考勤、消费、访客等8张表）

✅ **3. 索引验证脚本**
- 文件位置: `openspec/changes/performance-optimization-phase1/scripts/verify_indexes.sql`
- 功能: 验证索引创建成功，并测试索引效果

✅ **4. Caffeine本地缓存配置**
- 文件位置: `microservices/microservices-common-cache/src/main/java/net/lab1024/sa/common/config/CaffeineCacheConfig.java`
- 功能: 配置Caffeine本地缓存（L1级缓存）

✅ **5. Redis分布式缓存配置**
- 文件位置: `microservices/microservices-common-cache/src/main/java/net/lab1024/sa/common/config/RedisCacheConfig.java`
- 功能: 配置Redis分布式缓存（L2级缓存）

✅ **6. 三级缓存服务**
- 文件位置: `microservices/microservices-common-cache/src/main/java/net/lab1024/sa/common/service/ThreeLevelCacheService.java`
- 功能: 实现三级缓存架构（Caffeine + Redis + DB）

---

## 🚀 执行步骤

### Day 1: 数据库慢查询分析

**目标**: 识别需要优化的慢查询

#### 步骤1: 连接到数据库

```bash
# 方式1: 使用MySQL命令行
mysql -u root -p ioedream

# 方式2: 使用数据库工具（如Navicat、DataGrip）
# 连接到ioedream数据库
```

#### 步骤2: 执行慢查询分析脚本

```bash
# 在项目根目录执行
cd D:\IOE-DREAM

# 执行慢查询分析脚本
mysql -u root -p ioedream < openspec/changes/performance-optimization-phase1/scripts/analyze_slow_queries.sql

# 查看分析结果
# 结果将输出到控制台，包含：
# - 慢查询Top 20
# - 全表扫描的查询
# - 按表统计的慢查询
# - 缺失索引的表
```

#### 步骤3: 分析结果并记录

**关注重点**:
1. 平均响应时间超过1秒的查询
2. 扫描行数超过1万的查询
3. 缺失索引的表（❌ 无索引或 ⚠️ 索引不足）
4. 未使用的索引（可考虑删除）

**记录模板**:
```
慢查询Top 5:
1. SQL: _____________________
   平均响应时间: _______秒
   扫描行数: __________
   是否有索引: _______

2. SQL: _____________________
   平均响应时间: _______秒
   扫描行数: __________
   是否有索引: _______
...
```

---

### Day 2: 创建数据库索引

**目标**: 创建25个优化索引，提升查询性能70%

#### 步骤1: 备份数据库（⚠️ 重要！）

```bash
# 在创建索引前务必备份数据库
mysqldump -u root -p ioedream > backup_before_indexes_$(date +%Y%m%d).sql

# 验证备份文件
ls -lh backup_before_indexes_*.sql
```

#### 步骤2: 执行索引创建脚本

```bash
# 执行索引创建脚本
mysql -u root -p ioedream < openspec/changes/performance-optimization-phase1/scripts/create_indexes.sql

# 查看执行结果
# 应该看到：
# ✅ 门禁记录表索引创建完成
# ✅ 考勤记录表索引创建完成
# ✅ 消费记录表索引创建完成
# ...
```

#### 步骤3: 验证索引创建成功

```bash
# 执行索引验证脚本
mysql -u root -p ioedream < openspec/changes/performance-optimization-phase1/scripts/verify_indexes.sql

# 查看验证结果
# 应该看到：
# - 索引创建成功统计
# - 执行计划分析（type=ref表示使用了索引）
# - 索引选择性分析
# - 性能对比测试
```

#### 步骤4: 性能验证

**关键指标对比**:
| 指标 | 优化前 | 优化后 | 提升幅度 |
|------|--------|--------|----------|
| 查询响应时间 | 800ms | <200ms | 75% ↑ |
| 索引覆盖率 | 35% | 95% | 171% ↑ |
| 数据库CPU | 85% | <60% | 29% ↓ |

**验收标准**:
- ✅ 25个索引全部创建成功
- ✅ 查询响应时间降低70%以上
- ✅ EXPLAIN显示使用索引（type=ref或range）

---

### Day 3: 配置Caffeine本地缓存

**目标**: 配置L1级本地缓存，实现超高速数据访问

#### 步骤1: 验证配置文件已创建

```bash
# 检查Caffeine配置文件是否存在
ls -l microservices/microservices-common-cache/src/main/java/net/lab1024/sa/common/config/CaffeineCacheConfig.java

# 应该看到文件已创建
```

#### 步骤2: 添加Caffeine依赖

**文件**: `microservices/microservices-common-cache/pom.xml`

```xml
<!-- Caffeine缓存依赖 -->
<dependency>
    <groupId>com.github.ben-manes.caffeine</groupId>
    <artifactId>caffeine</artifactId>
    <version>3.1.8</version>
</dependency>
```

#### 步骤3: 编译验证

```bash
# 编译common-cache模块
cd D:\IOE-DREAM
mvn clean compile -pl microservices/microservices-common-cache -am

# 应该编译成功，无错误
```

#### 步骤4: 验证缓存配置

**检查项**:
- ✅ initialCapacity设置合理（避免rehash）
- ✅ maximumSize设置合理（防止内存溢出）
- ✅ expireAfterWrite设置合理（数据新鲜度）
- ✅ weakKeys启用（防止内存泄漏）
- ✅ recordStats启用（监控命中率）

---

### Day 4: 配置Redis分布式缓存

**目标**: 配置L2级分布式缓存，实现跨进程数据共享

#### 步骤1: 启动Redis服务

```bash
# Windows: 启动Redis服务
redis-server.exe

# 或使用Docker启动Redis
docker run -d -p 6379:6379 --name redis redis:7-alpine

# 验证Redis服务
redis-cli ping
# 应该返回: PONG
```

#### 步骤2: 配置application.yml

**文件**: `microservices/ioedream-gateway-service/src/main/resources/application.yml`

```yaml
spring:
  redis:
    host: localhost
    port: 6379
    password: ""  # 如果有密码则填写
    database: 0
    timeout: 5000ms
    lettuce:
      pool:
        max-active: 20
        max-idle: 10
        min-idle: 5
        max-wait: 3000ms
```

#### 步骤3: 验证Redis配置

```bash
# 连接Redis并验证连接
redis-cli

# 在Redis CLI中执行
127.0.0.1:6379> KEYS *
# 应该看到应用启动后创建的key

127.0.0.1:6379> GET cache:user:1
# 应该能读取到用户缓存数据（如果已存在）
```

---

### Day 5: 实现三级缓存服务

**目标**: 整合L1+L2+L3缓存架构，提升缓存命中率到90%+

#### 步骤1: 使用三级缓存服务

**示例代码**:

```java
@Service
public class UserServiceImpl implements UserService {

    @Resource
    private ThreeLevelCacheService threeLevelCacheService;

    @Resource
    private UserDao userDao;

    /**
     * 获取用户信息（使用三级缓存）
     */
    public UserBasicInfoVO getUserById(Long userId) {
        // 使用三级缓存获取用户信息
        return threeLevelCacheService.getUserBasicInfo(userId, (uid) -> {
            // L3: 数据库查询
            UserEntity entity = userDao.selectById(uid);
            if (entity == null) {
                return null;
            }
            return convertToVO(entity);
        });
    }

    /**
     * 更新用户信息（删除缓存）
     */
    public void updateUser(UserUpdateForm form) {
        // 更新数据库
        userDao.updateById(convertToEntity(form));

        // 删除缓存（L1 + L2）
        threeLevelCacheService.evictUserBasicInfo(form.getUserId());
    }
}
```

#### 步骤2: 测试三级缓存

**测试场景**:
1. **L1缓存命中**: 第一次查询后，立即再次查询，应该命中L1
2. **L2缓存命中**: 重启应用后查询，应该命中L2（Redis）
3. **L3数据库查询**: 清空Redis后查询，应该查询L3（数据库）
4. **缓存删除**: 更新数据后，L1和L2缓存应该被删除

**测试命令**:
```bash
# 测试L1缓存（同一进程内重复查询）
# 应该看到日志: [三级缓存] L1命中

# 测试L2缓存（重启应用后查询）
# 应该看到日志: [三级缓存] L2命中

# 测试L3查询（清空Redis后查询）
redis-cli FLUSHDB
# 应该看到日志: [三级缓存] L3查询
```

#### 步骤3: 查看缓存统计

```java
// 在Service中调用
String stats = threeLevelCacheService.getCacheStats();
log.info("[缓存统计]\n{}", stats);
```

**预期输出**:
```
=========================================
[三级缓存] 缓存统计信息
=========================================
用户基本信息缓存:
  - 命中率: 85.32%
  - 命中次数: 1234
  - 未命中次数: 213
  - 加载次数: 213

设备信息缓存:
  - 命中率: 90.15%
  - 命中次数: 567
  - 未命中次数: 62
  - 加载次数: 62
=========================================
```

**验收标准**:
- ✅ 缓存命中率 > 85%（目标90%）
- ✅ L1命中率 > 70%
- ✅ L2命中率 > 15%
- ✅ 无内存泄漏（weakKeys生效）

---

## 📊 Week 1 成果验证

### 最终验收指标

| 指标 | 目标值 | 实际值 | 达成率 |
|------|--------|--------|--------|
| **查询响应时间** | <200ms | ___ms | ___% |
| **索引覆盖率** | 95% | ___% | ___% |
| **缓存命中率** | >90% | ___% | ___% |
| **数据库CPU** | <60% | ___% | ___% |
| **L1缓存命中率** | >70% | ___% | ___% |

### 交付物清单

- [ ] 慢查询分析报告（从analyze_slow_queries.sql输出）
- [ ] 索引创建完成（25个索引全部创建成功）
- [ ] Caffeine缓存配置完成
- [ ] Redis缓存配置完成
- [ ] 三级缓存服务实现完成
- [ ] 性能测试报告（verify_indexes.sql输出）

---

## 🔄 回滚方案

如果出现问题，按以下步骤回滚：

### 回滚数据库索引

```bash
# 方式1: 删除新增的索引（保留原有索引）
mysql -u root -p ioedream < openspec/changes/performance-optimization-phase1/scripts/drop_indexes.sql

# 方式2: 从备份恢复
mysql -u root -p ioedream < backup_before_indexes_YYYYMMDD.sql
```

### 回滚缓存配置

```bash
# 1. 注释掉Caffeine和Redis配置类
# 在CaffeineCacheConfig.java和RedisCacheConfig.java类上添加
// @Configuration  // 注释此行

# 2. 重新编译
mvn clean compile

# 3. 重启服务
```

---

## 📞 支持和联系

**技术支持**:
- 架构师: ___________
- DBA团队: ___________
- 后端Leader: ___________

**问题上报**:
- 企业微信群: IOE-DREAM性能优化群
- 邮件: performance@example.com

---

## ✅ Week 1 完成检查清单

### 文档准备
- [ ] 慢查询分析脚本创建
- [ ] 索引创建脚本创建
- [ ] 索引验证脚本创建
- [ ] Caffeine缓存配置创建
- [ ] Redis缓存配置创建
- [ ] 三级缓存服务实现

### 实施验证
- [ ] 慢查询分析完成
- [ ] 25个索引创建成功
- [ ] 索引效果验证通过
- [ ] Caffeine缓存配置验证通过
- [ ] Redis缓存配置验证通过
- [ ] 三级缓存服务测试通过

### 性能验证
- [ ] 查询响应时间 <200ms
- [ ] 索引覆盖率 ≥95%
- [ ] 缓存命中率 ≥90%
- [ ] 数据库CPU <60%
- [ ] 无内存泄漏
- [ ] 无性能退化

---

**文档版本**: v1.0
**创建时间**: 2025-01-XX
**最后更新**: 2025-01-XX
**维护人**: 性能优化小组
