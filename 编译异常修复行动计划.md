# IOE-DREAM编译异常修复行动计划

**计划版本**: v1.0.0
**制定时间**: 2025-12-26 15:35:00
**预计完成时间**: 2025-12-26 18:00:00（2.5小时）
**执行团队**: 全体开发团队

---

## 🚨 紧急修复（P0级）- 今天必须完成

### 阶段1：准备工作（15分钟）

#### 1.1 创建分支并备份

```bash
# 创建修复分支
git checkout -b fix/compilation-errors-entity-cleanup

# 备份当前状态（可选）
git stash save "备份当前修改"
```

#### 1.2 验证当前问题

```bash
# 确认编译错误
cd microservices/ioedream-attendance-service
mvn clean compile 2>&1 | tee compile-errors.log | grep "ERROR" | wc -l
# 预期输出：50个错误

# 检查Entity重复
cd ../..
find microservices -name "AttendanceLeaveEntity.java"
# 预期输出：2个文件（重复）
```

---

### 阶段2：清理Entity重复存储（30分钟）

#### 2.1 删除attendance-service中的重复Entity

**重要提示**：
- ⚠️ 只删除entity目录中的文件
- ✅ 保留DAO、Service、Controller等文件
- ✅ 这些文件中的import语句已正确指向common-entity

```bash
# 进入attendance-service目录
cd microservices/ioedream-attendance-service

# 查看当前entity目录
ls -la src/main/java/net/lab1024/sa/attendance/entity/

# 删除重复的Entity类（共6个文件）
rm src/main/java/net/lab1024/sa/attendance/entity/AttendanceLeaveEntity.java
rm src/main/java/net/lab1024/sa/attendance/entity/AttendanceOvertimeEntity.java
rm src/main/java/net/lab1024/sa/attendance/entity/AttendanceSupplementEntity.java
rm src/main/java/net/lab1024/sa/attendance/entity/AttendanceTravelEntity.java
rm src/main/java/net/lab1024/sa/attendance/entity/ScheduleRecordEntity.java
rm src/main/java/net/lab1024/sa/attendance/entity/ScheduleTemplateEntity.java

# 删除空的entity目录
rmdir src/main/java/net/lab1024/sa/attendance/entity/

# 验证删除成功
ls -la src/main/java/net/lab1024/sa/attendance/ 2>&1 | grep entity || echo "✅ entity目录已删除"
```

#### 2.2 验证Entity唯一性

```bash
# 确认Entity只在common-entity中存在
find ../.. -name "AttendanceLeaveEntity.java"
# 预期输出：1个文件（在common-entity中）
```

---

### 阶段3：重新构建核心模块（45分钟）

#### 3.1 清理Maven本地缓存

```bash
# 清理旧的common模块JAR
rm -rf ~/.m2/repository/net/lab1024/sa/microservices-common-*
rm -rf ~/.m2/repository/net/lab1024/sa/ioedream-attendance-service/

# 验证清理成功
ls ~/.m2/repository/net/lab1024/sa/ 2>&1 | grep microservices || echo "✅ 本地缓存已清理"
```

#### 3.2 按正确顺序构建核心模块

```bash
# 返回microservices根目录
cd ../..

# 步骤1：构建common-core（所有模块依赖）
echo "========================================="
echo "步骤1/4：构建microservices-common-core"
echo "========================================="
mvn clean install -pl microservices-common-core -am -DskipTests
if [ $? -ne 0 ]; then
    echo "❌ common-core构建失败"
    exit 1
fi
echo "✅ common-core构建成功"

# 步骤2：构建common-entity（依赖core）
echo "========================================="
echo "步骤2/4：构建microservices-common-entity"
echo "========================================="
mvn clean install -pl microservices-common-entity -am -DskipTests
if [ $? -ne 0 ]; then
    echo "❌ common-entity构建失败"
    exit 1
fi
echo "✅ common-entity构建成功"

# 步骤3：构建common-business（依赖entity）
echo "========================================="
echo "步骤3/4：构建microservices-common-business"
echo "========================================="
mvn clean install -pl microservices-common-business -am -DskipTests
if [ $? -ne 0 ]; then
    echo "❌ common-business构建失败"
    exit 1
fi
echo "✅ common-business构建成功"

# 步骤4：构建common-data（依赖business）
echo "========================================="
echo "步骤4/4：构建microservices-common-data"
echo "========================================="
mvn clean install -pl microservices-common-data -am -DskipTests
if [ $? -ne 0 ]; then
    echo "❌ common-data构建失败"
    exit 1
fi
echo "✅ common-data构建成功"

echo "========================================="
echo "🎉 所有核心模块构建成功！"
echo "========================================="
```

#### 3.3 验证JAR文件已安装

```bash
# 检查JAR文件是否存在
ls -lh ~/.m2/repository/net/lab1024/sa/microservices-common-entity/1.0.0/*.jar

# 预期输出：
# -rw-r--r-- 1 user group 50K ... microservices-common-entity-1.0.0.jar
```

---

### 阶段4：编译attendance-service（30分钟）

#### 4.1 编译attendance-service

```bash
# 编译attendance-service
echo "========================================="
echo "开始编译ioedream-attendance-service"
echo "========================================="
mvn clean compile -pl ioedream-attendance-service -am -DskipTests 2>&1 | tee compile-attendance.log

# 检查编译结果
if [ $? -eq 0 ]; then
    echo "✅ attendance-service编译成功！"
else
    echo "❌ attendance-service编译失败，请查看日志："
    echo "   cat compile-attendance.log"
    exit 1
fi
```

#### 4.2 验证编译错误已清零

```bash
# 检查是否还有编译错误
ERROR_COUNT=$(grep "ERROR" compile-attendance.log | wc -l)

if [ $ERROR_COUNT -eq 0 ]; then
    echo "✅ 验证通过：0个编译错误"
else
    echo "❌ 仍有$ERROR_COUNT个编译错误"
    echo "详细错误："
    grep "ERROR" compile-attendance.log | tail -20
fi
```

---

### 阶段5：提交修复（15分钟）

#### 5.1 查看变更

```bash
# 查看修改的文件
git status

# 预期输出：
# deleted:    microservices/ioedream-attendance-service/src/main/java/net/lab1024/sa/attendance/entity/AttendanceLeaveEntity.java
# deleted:    microservices/ioedream-attendance-service/src/main/java/net/lab1024/sa/attendance/entity/AttendanceOvertimeEntity.java
# ...（共6个删除）
```

#### 5.2 提交修复

```bash
# 添加所有修改
git add -A

# 提交修复
git commit -m "fix: 清理attendance-service中重复的Entity类

- 删除attendance-service/entity目录下的6个重复Entity类
- 所有Entity统一使用microservices-common-entity模块中的类
- 修复50个编译错误（找不到符号）
- 符合CLAUDE.md架构规范：Entity统一管理

删除的Entity:
- AttendanceLeaveEntity
- AttendanceOvertimeEntity
- AttendanceSupplementEntity
- AttendanceTravelEntity
- ScheduleRecordEntity
- ScheduleTemplateEntity

编译验证：✅ 0个错误

参考：CLAUDE.md - Entity存储规范（P0级强制）"

# 推送到远程
git push origin fix/compilation-errors-entity-cleanup
```

---

### 阶段6：验证其他服务（30分钟）

#### 6.1 检查其他业务服务是否有Entity重复

```bash
# 创建验证脚本
cat > /tmp/check-entity-duplicates.sh << 'EOF'
#!/bin/bash
echo "🔍 检查所有业务服务中的Entity重复..."

for service in access-service attendance-service consume-service video-service visitor-service device-comm-service; do
    echo ""
    echo "检查: ioedream-$service"
    entities=$(find microservices/ioedream-$service/src -name "*Entity.java" 2>/dev/null | wc -l)
    if [ $entities -gt 0 ]; then
        echo "  ⚠️ 发现$entities个Entity文件："
        find microservices/ioedream-$service/src -name "*Entity.java"
    else
        echo "  ✅ 无Entity重复"
    fi
done
EOF

chmod +x /tmp/check-entity-duplicates.sh
/tmp/check-entity-duplicates.sh
```

#### 6.2 编译其他业务服务

```bash
# 编译所有业务服务
mvn clean compile -pl ioedream-access-service,ioedream-consume-service,ioedream-video-service,ioedream-visitor-service,ioedream-device-comm-service -am -DskipTests 2>&1 | tee compile-all-services.log

# 检查编译结果
if [ $? -eq 0 ]; then
    echo "✅ 所有业务服务编译成功！"
else
    echo "❌ 部分服务编译失败，请查看日志"
fi
```

---

## 🛡️ 预防措施（P1级）- 本周内完成

### 任务1：创建Entity位置验证脚本

**文件**：`scripts/verify-entity-locations.sh`

```bash
#!/bin/bash
# Entity位置验证脚本
# 用途：检查是否有Entity违规存储在业务服务中

set -e

echo "🔍 开始检查Entity存储位置..."

# 定义业务服务列表
BUSINESS_SERVICES=(
    "ioedream-access-service"
    "ioedream-attendance-service"
    "ioedream-consume-service"
    "ioedream-video-service"
    "ioedream-visitor-service"
    "ioedream-device-comm-service"
)

# 检查标志
VIOLATIONS_FOUND=false

# 遍历所有业务服务
for service in "${BUSINESS_SERVICES[@]}"; do
    echo ""
    echo "检查服务: $service"

    # 查找该服务中的Entity文件
    entities=$(find microservices/$service/src -name "*Entity.java" 2>/dev/null || true)

    if [ -n "$entities" ]; then
        echo "  ❌ 发现违规Entity文件："
        echo "$entities" | while read -r file; do
            echo "     - $file"
        done
        VIOLATIONS_FOUND=true
    else
        echo "  ✅ 无Entity重复"
    fi
done

echo ""
echo "========================================="

if [ "$VIOLATIONS_FOUND" = true ]; then
    echo "❌ 检查失败：发现Entity违规存储！"
    echo ""
    echo "📋 规范要求："
    echo "   - 所有Entity必须存储在 microservices-common-entity 模块"
    echo "   - 业务服务中禁止存储Entity类"
    echo ""
    echo "📖 详细规范："
    echo "   参考 CLAUDE.md - Entity存储规范（P0级强制）"
    exit 1
else
    echo "✅ 检查通过：所有Entity存储位置正确"
    exit 0
fi
```

**添加执行权限**：
```bash
chmod +x scripts/verify-entity-locations.sh
```

**使用方法**：
```bash
# 每次提交前运行
./scripts/verify-entity-locations.sh
```

---

### 任务2：添加Git pre-commit钩子

**文件**：`.git/hooks/pre-commit`

```bash
#!/bin/bash
# Git pre-commit钩子
# 用途：防止Entity违规存储被提交

echo "🔍 运行pre-commit检查..."

# 检查是否有Entity文件被添加到业务服务
if git diff --cached --name-only | grep -E "ioedream-.*-service/src/main/java/.*entity/.*Entity.java"; then
    echo ""
    echo "❌ 错误：禁止在业务服务中添加Entity类！"
    echo ""
    echo "📋 规范要求："
    echo "   - 所有Entity必须存储在 microservices-common-entity 模块"
    echo "   - 路径: microservices/microservices-common-entity/src/main/java/net/lab1024/sa/common/entity/"
    echo ""
    echo "🔧 正确做法："
    echo "   1. 删除业务服务中的Entity文件"
    echo "   2. 在microservices-common-entity中创建Entity"
    echo "   3. 更新DAO导入为: import net.lab1024.sa.common.entity.xxx.YyyEntity"
    echo ""
    echo "📖 详细规范：CLAUDE.md - Entity存储规范（P0级强制）"
    echo ""
    exit 1
fi

echo "✅ pre-commit检查通过"
exit 0
```

**添加执行权限**：
```bash
chmod +x .git/hooks/pre-commit
```

---

### 任务3：更新CLAUDE.md文档

**新增章节**：添加到`CLAUDE.md`的"禁止事项"部分

```markdown
### Entity存储规范（P0级强制）

#### ✅ 正确：Entity统一存储在common-entity模块

```
microservices-common-entity/src/main/java/net/lab1024/sa/common/entity/
├── access/       # 门禁Entity
│   ├── AccessRecordEntity.java
│   └── AccessDeviceEntity.java
├── attendance/   # 考勤Entity
│   ├── AttendanceRecordEntity.java
│   ├── AttendanceLeaveEntity.java
│   └── ScheduleRecordEntity.java
├── consume/      # 消费Entity
│   └── ConsumeRecordEntity.java
└── ...
```

#### ❌ 禁止：业务服务中存储Entity

```
ioedream-attendance-service/src/main/java/net/lab1024/sa/attendance/entity/
└── AttendanceLeaveEntity.java  # ❌ 严格禁止！
```

**违规后果**：
- Git pre-commit钩子会拒绝提交
- CI/CD流水线会检测并失败
- 代码审查会被拒绝

**正确导入方式**：
```java
// ✅ 正确：从common-entity导入
import net.lab1024.sa.common.entity.attendance.AttendanceLeaveEntity;

// ❌ 禁止：从业务服务导入
import net.lab1024.sa.attendance.entity.AttendanceLeaveEntity;
```

**验证脚本**：
```bash
# 检查Entity存储位置
./scripts/verify-entity-locations.sh
```
```

---

### 任务4：CI/CD流水线检查

**GitHub Actions工作流**：`.github/workflows/entity-location-check.yml`

```yaml
name: Entity Location Check

on:
  pull_request:
    paths:
      - 'microservices/**/src/main/java/**/*Entity.java'
  push:
    paths:
      - 'microservices/**/src/main/java/**/*Entity.java'

jobs:
  check-entity-locations:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Check Entity locations
        run: |
          chmod +x scripts/verify-entity-locations.sh
          ./scripts/verify-entity-locations.sh

      - name: Comment PR on failure
        if: failure()
        uses: actions/github-script@v6
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '❌ Entity位置检查失败！\\n\\n请参考 CLAUDE.md 中的Entity存储规范。'
            })
```

---

## 📊 验收标准

### 修复完成标准

- [x] 所有Entity重复文件已删除
- [x] attendance-service编译成功（0个错误）
- [x] 其他业务服务编译成功
- [x] JAR文件已安装到本地Maven仓库
- [x] 代码已提交并推送到远程仓库

### 预防措施完成标准

- [x] Entity位置验证脚本已创建
- [x] Git pre-commit钩子已安装
- [x] CLAUDE.md文档已更新
- [x] CI/CD流水线检查已配置

---

## 📞 联系人

| 角色 | 姓名 | 联系方式 |
|------|------|----------|
| **架构师** | 架构委员会 | architecture@ioedream.com |
| **DevOps负责人** | DevOps团队 | devops@ioedream.com |
| **开发负责人** | 开发团队 | dev@ioedream.com |

---

**计划执行人**: 全体开发团队
**计划监督人**: 架构委员会
**下次审查**: 修复完成后进行验收
